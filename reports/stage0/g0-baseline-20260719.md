# G0 基线核验摘要（2026-07-19）

- 总状态：`BLOCKED`
- G0-C：`PASS`
- G0-G：`BLOCKED`
- 机器可读真值：`reports/stage0/g0-baseline-20260719.json`

## G0-C

现有 SSH 别名在 BatchMode 下通过；本机基线、GitHub `main` 和服务器 `main` 均从提交 `34966d0819a5229569169bfe436afe9058c0ed24` 起步，服务器工作树干净。Stage 0 在 `feat/stage0-infrastructure` 分支实施，用户已有文档、计划和工作日志改动均保留。

两端 `Agent/` 都恰好包含规定的五份文件。审查确认差异仅为本机 `worklogs.md` 对中文记录要求的明确化；以本机版本为权威精确同步后，五份文件的大小与 SHA-256 已完全一致。

## G0-G

GPU gate 未通过。PCI 与驱动目录见 8 张 A100，NVML/PyTorch 仅见 7 张，缺失 PCI `0000:4f:00.0`。当前映射到可见 index 0 的 `0000:50:00.0` 有 24 次不可纠正 ECC、row-remap pending，并出现 Xid 95。只读健康查询后已停止进一步 GPU 探测；没有执行 reset、驱动重载、重启或训练。

项目所有者已确认只需要四张 GPU，并选择路径 2；项目范围状态为
`PROJECT_OWNER_APPROVED_ADMIN_PENDING`：

1. 恢复 PCI/驱动、NVML、PyTorch 的 8/8/8 一致枚举，并复验健康四卡；
2. 批准稳定降配，提供可用设备 PCI/UUID 白名单和被排除设备的处置结论，再复验运行时映射与健康四卡。

因此不再要求为项目恢复全部八卡，但管理员仍须实际强制路径 2，且
`0000:4f:00.0`、`0000:50:00.0` 默认排除。单纯使用四个数字 index 不能解释或修复
8/7 差异，也不能使 G0-G 通过。

在此之前，S0.2–S0.5 的非 GPU 工作可以继续；任何 CUDA、NCCL、DDP、显存或正式训练测试均禁止执行。旧 `READY` 不作本轮证据。
