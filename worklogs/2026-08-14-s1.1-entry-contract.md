# 2026-08-14 S1.1 入口条件与数学合同

## 范围

- 时间：2026-08-14 08:58:53 +08:00（Asia/Shanghai）。
- 当前任务：补齐 `stage1.01_entry_and_contract`，并为正式 S1.1 运行准备 G1-ENTRY / G1-CONTRACT 证据。
- 基线提交：`a15f0e2970b7cae6951dd606ebd396a8df68255c`；实现尚未提交。
- 本次未执行真实服务器资产下载、训练或 GPU 占用变更。

## 已完成

1. 入口快照现在记录 Git 分支/HEAD/工作树、精确五个 `Agent/` 文件及 SHA-256、DATA_ROOT 磁盘/ inode 可见信息、缓存变量绑定、写路径审计、运行时 CUDA/NCCL 可见性，以及由 Stage 0 G10 承接的进程/GPU/NCCL 外部证据引用。
2. S1.1 数学合同冻结了目标量、符号方向、Raw/Double/Equal-U 公式、实际消耗学习率、裁剪顺序、有效 target-token loss reduction、step 生命周期、data movement 与 weight decay 边界。
3. 正式 scope 生成 Stage 1 `ContractFreeze`、G1-ENTRY 和 G1-CONTRACT 两个嵌套 `gate-record-v1`；local fixture 明确只生成 NOT_RUN 候选，不能升级为正式 Gate。
4. 修正任务目录的循环依赖：S1.1 formal 只要求 Stage 0 合同和 `stage0.G10`，Stage 1 合同由 S1.1 自身冻结。
5. formal preflight 的 contract payload 提取支持多层嵌套；Gate 提取按 Gate ID 递归查找，允许后续任务复用同一个 S1.1 Gate artifact。

## 验证

- 受影响回归集合：`76 passed`。
- 新增 S1.1 测试：`2 passed`，覆盖 local 永不 formal、formal 合同/Gate 发布及下一任务 preflight 消费。
- `git diff --check`：通过。
- pytest 默认临时目录存在既有 Windows ACL 警告；验证使用工作区内隔离临时根完成，未把该环境问题计入实现失败。

## 后续审计修正（新证据政策生效后）

1. 上述“实现尚未提交”是当时状态；实现后来已落在 `54b1c7f`。本段保留历史事实，不把旧状态冒充当前结论。
2. 原“因新提交而重新执行 Stage 0 G0-G9”的要求已由 `policies/evidence-validity-and-rerun.md` 废止。当前审核应保留 `a15f0e2970b7cae6951dd606ebd396a8df68255c` 生成的 G10，不重写旧产物；只对 G10 生成提交到 S1.1 消费提交的完整差异发布复用证明。
3. S1.1 是 CPU/契约边界，不重新占用 GPU 或运行 NCCL。服务器只运行 S1.1 formal 并确认 G1-ENTRY、G1-CONTRACT；单卡和多卡健康重验分别推迟到 S1.7、S1.8 的真实消费边界。
4. 正式完成后的提交、索引、Gate hash 和验证结果追加在本日志末尾；追加日志本身属于下游元数据，不使已发布 S1.1 证据失效。

## 2026-08-14 14:34 CST — S1.1 正式完成

- 状态：S1.1 已于 `2026-08-14T06:34:11Z` 正式完成，唯一正式 attempt 为 `formal-20260814-r1`；未重跑 Stage 0，未调用 GPU、CUDA 或 NCCL。
- Stage 0 G10 生产提交：`a15f0e2970b7cae6951dd606ebd396a8df68255c`；S1.1 正式消费提交：`f512b3b52dca2d5ffdb5a8ab635209abb189a188`。
- 完整差异复用证明：`evidence/stage1/s1-1-reuse/f512b3b52dca2d5ffdb5a8ab635209abb189a188/attestation.json`；44 个变更路径全部审查，artifact hash 为 `87467ad8ffdda974593dcb404f463c52879eae0788bdc741ee78ebe3437003e9`，文件 SHA-256 为 `9fa581b222e1f4c1b1c8f47218531cebf0b31e407644bbfd2d278e13e9587dd9`。
- 复用审查文件 SHA-256：`9f39aee72d487706a415baa211865301cd6b85f54281cca4d19c4a67170f1f63`。
- 服务器回归：`119 passed`；JUnit 文件为 `evidence/stage1/s1-1-validation/f512b3b52dca2d5ffdb5a8ab635209abb189a188/attempt-1/pytest.xml`，SHA-256 为 `d5eee599323a114fcc3f51b959c4f5233ca01a7ab5227da6d25183f65f11b55b`。本地扩展回归为 `170 passed, 1 skipped`；另一次 Windows 测试启动因指定临时目录父级不存在产生 setup error，改用既有系统临时根后全部通过，未计为实现失败或正式 attempt。
- 正式索引：`evidence/stage1/s1-1-formal/f512b3b52dca2d5ffdb5a8ab635209abb189a188/formal-20260814-r1/index.json`；artifact hash 为 `e1be1532f18c139f31e8e94a2b16a1d5060e459b8b0a1b01f7c8ae74ca87a5cc`，文件 SHA-256 为 `a1ed6d85f8995b560b91157e9d16d7150323aca98749e8b576aa064a3232c522`。
- 正式配置 hash：`4519664fd590368ce5d71cd993a209cc98bfc56ee5bdd82d0551c497949d5411`；环境 hash：`070007e6f494abb9c6c0bcfd392bcdeb90c4cc3319bbe8f10fe582495e8f47a7`；结果 hash：`6f48f4728c14e74ed25c91e99bb402eeed129c8e2e6432b54b66dd0d042326e9`。
- `stage1.G1-ENTRY`：PASS，artifact hash `84ffa184d171d1de83ac2899448882f962647dde4019ceed2643da505fb7c0c5`。
- `stage1.G1-CONTRACT`：PASS，artifact hash `f1bc36a05a5e33cd082df6fca2f412d82c3d83ecd7232d91ba0aeee7ce905771`。
- 下一任务：`stage1.02_architecture_and_parameter_registry`（S1.2）。本日志追加提交仅记录已经完成的正式结果，不改变 S1.1 生产提交或证据有效性。
