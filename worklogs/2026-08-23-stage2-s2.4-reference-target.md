# 2026-08-23 — S2.4 formal TaskRuntime reference sizing、one-shot A/B 与 uncertainty

## 范围与身份

本日志记录 `feat/stage2-s204-formal` 在基线 `9c85972bc0fd2bc999dddefe16f735536cd6769b` 上的 S2.4 实现。当前入口是严格的 TaskRuntime formal provider；`--plan-only` 只用于 preflight，`--execute` 逐 cell 调用真实 `stage2.04_reference_target`，不制造 synthetic 证据。代码、schema 和测试 producer commit 由本分支交付回报记录；未修改主工作树中的 presentation 用户文件。

本实现只消费已经冻结的上游 identity：S2.1 producer `c774b27e5005d59840e3c5844fbf690ccf47d733`；S2.2 code producer `3fa53781a1cf054ec4dc11492c22bff794a23a53`，以及其所消费的 Stage 1/S1.11 formal producer `3f18b04df8922be9894678ae4842bd999c7e8fd5`；S2.3 最终 code/execution producer `676ab436422b1a514bddfc1181d0645fed4de7be`。本任务没有改预注册 margin、checkpoint、数据范围或抽样 identity。

## 实现与消费契约

- `reference_sizing` 是独立 sizing stream；只有 sizing 收敛后才创建固定的 `OneShotReferencePlan`。
- final reference 严格消费独立的 `reference_A` 与 `reference_B` prefix，固定样本量、block 顺序和 draw identity；没有 optional stopping、replacement 或 synthetic fallback。
- `ReferenceUncertainty` 以 block 为统计单位，保存 bias/cross/ranking 的 delete-one block jackknife 方差、block counts 和固定置信水平；`estimate_sequence_variance` 单独报告 sequence-level variance，避免把 reference sampling uncertainty 和序列噪声混为一谈。
- `stage2-reference-uncertainty-v1`、`stage2-reference-one-shot-plan-v1`、`stage2-reference-one-shot-result-v1` schema 与 `validate_stage23_artifact` fail-closed 合同已接入。one-shot result 强制绑定 sizing hash、provider/registry、A/B draw hashes、uncertainty 与 sequence variance hash。
- sizing 与 final 均使用 immutable tensor object + 独立 progress commit。恢复时校验 plan/provider/registry/weighting/draw identity；`scientific_artifact_hash` 排除仅表示执行历史的 `resumed_from_block_pairs`，保证重启不会改变 one-shot plan 或正式结果身份。任务级 recovery 保留既有 `authoritative_block_pair_commits` 字段，并以 `reference_protocol` 明示 sizing/final 两段提交。

## 测试与证据

开发验收使用本地 fixture provider，仅证明代码路径、schema、恢复和身份合同，不产生正式科学结论。最终组合套件：

```text
python -m pytest tests/test_stage2_s204_reference.py tests/test_stage23_formal_orchestration.py tests/test_stage23_task_runners.py tests/test_stage2_s202_handoff_contract.py tests/test_stage2_formal_handoff_adapter.py -q
57 passed in 52.83s
```

`python -m py_compile` 对本次修改的 formal、runner、contract 文件通过。开发中曾出现并已定位修复的失败：s204-08 为 dataclass 默认字段位于非默认字段前；s204-09 为新 contract 分支缺失 `_require_int`；首次 final 组合为 55 passed/2 failed，分别是旧 recovery 字段兼容断言和 resume-only sizing hash 漂移。随后读取已有 task artifacts/manifest，并以最小函数级断言定位科学 hash 与任务 metadata 同时需要稳定化；未重复启动同一失败单测。所有 pytest temp 已按 `Agent/local_temp.md` 清理到主仓库 `.agent-temp/pytest/`，worktree 根没有测试临时目录或诊断脚本。

## Gate、服务器与未决项

本轮没有服务器正式运行，没有声明 G2.0/G2.1/G2.2 已通过，也没有把 local fixture 伪装成正式证据。任务输出保持 `formal_eligible=false`、Gate `NOT_RUN`。已知 Stage 0 单副本风险接受只到 2026-08-18，当前日期已过期；在获得新的风险授权并完成 G2.0/G2.1/G2.2 复核、服务器资产/抽样验收和正式 evidence publish 前，G2.3 只能记为 `PENDING_EXTERNAL_AUTHORIZATION`，不得声明 PASS。

因此本日志的状态是：S2.4 代码/schema/本地恢复验收已交付给主代理审阅；正式 G2.3 仍阻塞于最小外部授权与正式服务器证据，S2.4 不标记为正式完成，也不标记为跳过。

## 2026-08-23 17:13 CST — 正式入口缺口与可恢复 preflight

### 目标与范围

- 在基线 `9c85972bc0fd2bc999dddefe16f735536cd6769b` 的隔离 worktree 中，仅补 S2.4 formal reference 的入口与前置身份校验；不触碰 S2.5/S2.6/S2.7。
- 固定消费 G2.1 `259831e2a1b16afbbef34c9cea602e636756b0f6173d1a8f4c32ec554c653f79`、S2.3 asset digest `f57decd5cf00e69e45ab2f02c994abb202f5c614e1441acb8aebcb1807ff76ee`、data digest `df8eeac5178305d409cf6128ac5d5648567aae895592c79fa21542e84a28e0f1`，并强制排除 PCI `0000:50:00.0`。

### 实际修改

- 新增 `ops/stage2/run_s204_formal.py`：`--plan-only` 严格校验 G2.1/S2.3 身份，列出六主 cell、512 起始翻倍 sizing 阶梯、block size、固定工作量与 progress/heartbeat 路径；在未有真实 fixed-state provider command 时 execute mode fail-closed，不创建 synthetic 或伪造 `B_ref`。
- 新增 `tests/test_s204_formal_launcher.py`：覆盖六 cell/未冻结 `B_ref` 与故障 GPU 排除。
- 根工作树 `presentation/` 用户修改未触碰、未暂存。

### 验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| launcher 定向测试 | `python -m pytest tests/test_s204_formal_launcher.py -q` | 2 passed, exit 0 | `.agent-temp/pytest/s204-launcher-02/` |
| 语法与差异 | `python -m py_compile ...; git diff --check` | PASS | 隔离 worktree |
| 服务器只读预检 | 健康 GPU、挂载、空间、进程、资产 manifest | 8 卡空闲；PCI `0000:50:00.0` 排除；storage 3.5T/45%；无 formal reference 进程；六 checkpoint READY | 服务器只读命令输出 |

### Gate 与执行状态

- 只读估算：六 cell 至少各消费 sizing 阶梯上限及固定 A/B 全长，保守数量级为数小时，超过 10 分钟；尚未冻结 `B_ref`，因此没有合法的正式 duration 或六 cell 数值结果。
- 当前 formal task runner 存在内部 `StreamingReferenceSizer`/`OneShotReferenceRunner`，但缺少可直接消费服务器 G2.1/S2.3 manifest 的入口；本次新增入口只允许计划/preflight，未启动梯度，也不宣称 G2.3。
- G2.3：`NOT_RUN/PENDING_FORMAL_PROVIDER_ENTRY`；未生成正式 reference 路径/hash，不得把 plan-only JSON 当作科学 reference。

### 下一步

- 提交并推送本 S2.4 producer，精确 bundle 同步服务器；由主代理按可恢复命令先执行 `--plan-only` 和 provider/环境 preflight，再决定是否启动正式 sizing/one-shot。执行必须保持 append-only/atomic 输出、一次性 A/B、无 optional stopping/重抽/延长。

## 2026-08-23 18:xx CST — TaskRuntime 正式入口与 G2.3 fail-closed qualification hook

### 入口修正

- 移除 launcher 内直接构造 `OfflineHuggingFaceModelAdapter`、Pile resolver、Torch provider 和伪造 `FormalExecutionEvidence/GateRecord` 的路径；真实 `--execute` 现在只接受六份（或单 cell 恢复一份）已解析 formal Stage2.04 config 与一个 hash-bound `TaskRuntimeEnvironment`，逐 cell 调用仓库既有 `TaskRuntime.stage2.04_reference_target` specialized runner。
- 因此 Stage0 `require_ready`/G3 runtime resolution、Stage1 G1-EXIT、G2.1/G2.2 完整 predecessor commits、contract freeze、正式抽样/数据/checkpoint identity 与 provider lineage 均由既有正式 loader 检查；launcher 不降级到 fixture/synthetic。
- `CUDA_VISIBLE_DEVICES` 默认固定为 smoke-approved `0,2,3,4`，任何包含坏卡索引 `1`（PCI `0000:50:00.0`）或未批准索引的集合均拒绝；每 cell 保留 `IN_PROGRESS`、TaskRuntime result、`recovery.resume_ref` 与 append-only heartbeat。

### G2.3 资格化

- 新增严格 G2.3 evaluator hook：要求六 cell 的 normalized-L1、Pearson、signal-eligible/ layer-module Spearman、0.1%/1%/5% top-k、layer/module delta、`h_ref <= min(delta_sci)/4`、`epsilon_num <= min(delta_sci)/10`、A/B 区间覆盖、bias/cross 覆盖、ranking 正偏方向、variance scaling、state replay 与 one-shot 完成等全部证据。
- 缺少独立 metrics artifact、任一字段缺失/非有限、阈值失败或 cell 不全均只写 `stage2.G2.3=BLOCKED` 与 `formal_eligible=false`；不改变 delta/数值阈值，不延长/重抽，也不把 runner candidate 或 local fixture 冒充 PASS。Gate/qualification 采用 atomic immutable publish。

### 验证

```text
python -m py_compile ops/stage2/run_s204_formal.py
pytest -q --basetemp=.agent-temp/pytest/s204-formal-runtime tests/test_s204_formal_launcher.py
5 passed
pytest -q --basetemp=.agent-temp/pytest/s204-formal-regression tests/test_s204_formal_launcher.py tests/test_stage2_s204_reference.py tests/test_stage23_formal_orchestration.py tests/test_stage23_task_runners.py
44 passed in 128.35s
```

本轮仍未启动服务器正式梯度，因此没有声明 G2.3 PASS；可执行命令必须由主代理绑定真实 config/environment/metrics evidence 后启动。正式运行预估仍为 >10 分钟（保守数小时）。

## 2026-08-23 19:xx CST — 单 cell/单卡安全与 aggregate 分离修正

### 入口与设备安全

- `--execute` 现在强制 `--cell-id`、单份 resolved config、单个物理 GPU；四卡并行必须由四个独立进程承担，不能在一个进程里暴露多个卡或交叉调度 cell。
- 执行前从 G2.1 `current_gpu_smoke.ref` 按其 `sha256` 在 `DATA_ROOT` 内重读原始 smoke report，核对排除 PCI `0000:50:00.0`/UUID `GPU-dc6cfc60-41dd-7bcf-ed09-b7deb5be342c` 与批准四卡 PCI/UUID 集；再读取 live `nvidia-smi index,pci.bus_id,uuid`，禁止仅信任物理 index。最终通过 UUID 设置 `CUDA_VISIBLE_DEVICES`，并将原始 smoke 引用、live inventory 与 inventory hash 写入 cell attempt。
- cell attempt 使用 `fresh-<config.full_hash>` 或 `resume-<config.full_hash>` 内容身份，`recovery.resume_ref` 不可在 fresh/resume 间静默互换；结果、artifact refs、配置 hash、六主 cell identity、GPU inventory 均回绑到该 attempt。

### 执行/汇总边界

- `execute` 只调用现有 `TaskRuntime.stage2.04_reference_target`，发布该 cell 的 TaskRunResult、状态与 heartbeat，永不调用 G2.3 gate，也不写固定汇总文件。
- `aggregate` 是单写者模式：发现六个 COMPLETE 后，重新加载 TaskRunResult、resolved config 和 formal artifact commits，严格检查模型 asset/revision/initialization/architecture、数据 asset/revision/sequence length、provider registry 与 tensor bundle hash；缺 cell、重复成功身份或绑定失败只发布内容寻址 preflight，不锁死后续合法 retry。
- G2.3 数值 hook 保留为独立 evaluator 的 fail-closed 适配；metrics 必须绑定六个 task result/bundle hash 与 calculator producer identity，gate/qualification 使用 metrics digest 内容寻址目录。当前没有服务器 formal execution、reference artifact hash 或 G2.3 PASS。
- 在独立 output-derived evaluator 接入前，即使 caller 提供数字 metrics，launcher 也追加 `G2.3_OUTPUT_DERIVED_EVALUATOR_NOT_BOUND` 并只发布 BLOCKED candidate，绝不把人工/外部数字 JSON 升格为正式 Gate。

### 验证

```text
python -m py_compile ops/stage2/run_s204_formal.py
pytest -q --basetemp=.agent-temp/pytest/s204-formal-adapter2 tests/test_s204_formal_launcher.py
8 passed
pytest -q --basetemp=.agent-temp/pytest/s204-formal-final tests/test_s204_formal_launcher.py tests/test_stage2_s204_reference.py tests/test_stage23_formal_orchestration.py tests/test_stage23_task_runners.py
47 passed in 249.96s
```

## 2026-08-23 20:xx CST — 独立审阅修正：正式 manifest、健康卡与不可变 attempt

### 本轮修正

- `main` 现在始终调用 `load_g21_formal_handoff`；execute/aggregate 还会用 `DATA_ROOT` 重读并按声明 SHA-256 校验 bound raw smoke。handoff 缺少 `excluded_device` 时不臆造字段；若字段存在才核验 PCI/UUID，raw report 必须含完整 excluded device、原子发布与批准四卡集合。
- S2.3 输入改用 `AssetResolutionManifest.from_mapping`、`DataRangeManifest.from_mapping` 与 `validate_formal_asset_identity`，并要求两个独立 data-range manifest 完全一致、digest 精确匹配。checkpoint rows 不再读取不存在的 `asset_id`/`initialization_id`/`architecture`；由冻结 model/stage/step selection 映射到 G3 logical asset、base initialization 与 `identity.input_checkpoint_id`。
- live `nvidia-smi` 读取坏卡+四批准卡完整 PCI/UUID 子集、显存/利用率、ECC、recovery health 与 compute-app；不满足空闲、健康或集合完整性即拒绝。status 使用 append-only attempt events + immutable final status；每个 resolved `artifacts.output_dir` 有独占 lease，阻断重复 fresh/resume writer。
- aggregate 重新验证 TaskRunResult 的 task/stage/runner/run_intent/config hash/formal eligibility/recovery、cell identity、`input_checkpoint_id`；实际 `load_tensor_bundle` 后校验 manifest 和 bias/cross/ranking/sequence-variance 向量 digest，结果 artifact refs 不能只依赖字符串长度。

### 验证

```text
python -m py_compile ops/stage2/run_s204_formal.py
pytest -q --basetemp=.agent-temp/pytest/s204-review tests/test_s204_formal_launcher.py tests/test_stage2_assets_checkpoints_sampling.py tests/test_g21_formal_handoff.py
21 passed
pytest -q --basetemp=.agent-temp/pytest/s204-review-runtime tests/test_stage23_task_runners.py tests/test_task_artifacts_and_default_runners.py
32 passed in 60.08s
```

追加组合回归（含上述 launcher/manifest/G2.1 用例）为 `53 passed in 60.66s`；未启动 GPU。

本轮未启动 GPU、未升级依赖、未发布正式 reference 或 G2.3 Gate；正式结果仍须由主代理在服务器绑定真实 predecessor/config/environment 后执行。当前 launcher 的数值 Gate 仍 fail-closed，等待独立 output-derived evaluator。

## 2026-08-23 追加 — 八卡主机的 selected-GPU 健康边界（P0 修正）

主代理独立只读 `nvidia-smi` 验证记录：live inventory 共 8 卡；坏卡 PCI `0000:50:00.0`、UUID `GPU-dc6cfc60-41dd-7bcf-ed09-b7deb5be342c` 的 ECC volatile 为 `113`、aggregate 为 `179`，recovery 为 `Drain and Reset`。该卡仍必须出现在完整 PCI/UUID 身份集合中且永远不可 selected，但这些已知坏卡状态不能使批准卡的正式启动被错误阻断。

本轮最小修正 `_validate_live_gpu_health(..., selected=...)`：

- 完整 inventory 仍严格要求四批准卡与一坏卡的 PCI/UUID 映射，且所有 live PCI/UUID 不重复；坏卡和其余非批准卡只做身份/排除审计，不做健康/空闲判定。
- 每个独立 cell 进程只核验其 selected PCI/UUID 的显存、利用率、ECC、recovery 与 compute-app；其他批准卡或其他三张非批准卡上的进程不会阻断当前 selected 卡。
- selected 卡自身 busy、ECC 非零、recovery 非 clean 或存在 compute-app 仍 fail closed；选择坏卡仍立即拒绝。

新增真实八卡形状测试覆盖：坏卡异常 ECC/recovery 可通过身份层；其他批准/非批准卡 busy/app 不影响 selected；selected busy/ECC/app 失败；坏卡选择失败。未启动 GPU、未放宽 selected 健康门槛。

## 2026-08-25 09:24 CST — S2.4 sizing shard blocker recovery

### 目标与范围

- 仅诊断并修复 pythia-14m 初始化路径在 `stage2_reference_sizing` 的首个确定性阻断；未触碰 provider performance code、G3 或服务器正式运行。

### 根因与实际修改

- `StreamingReferenceSizer.run` 在首个收敛候选 B 后由 `while processed_pairs < total_pairs and selected is None` 提前停止；因此后续预注册候选 B 没有 authoritative shard commits。
- `_derive_sizing_delta_sci` 对每个候选 B 的 raw sizing shards 做严格覆盖检查，命中 `sizing shards do not cover every candidate B` 是正确的科学门槛，不是应放宽的校验。
- 修改 `src/param_importance_nlp/experiments/stage2_formal.py`：继续发布至最大候选 B，同时仅在 `selected is None` 时冻结首个收敛候选，保留原始 convergence 语义。
- 修改 `tests/test_stage23_formal_orchestration.py`：验证首个收敛 B=8 保持不变，processed sizing 达到 12 且 commits 覆盖 4/8/12。
- 用户已有 presentation 文件保持原样未纳入本轮修改。

### 验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 定向恢复测试 | `python -m pytest tests/test_stage23_formal_orchestration.py::test_streaming_reference_sizing_resumes_from_authoritative_block_commit ... -q` | 1 passed，exit 0 | `.agent-temp/pytest/s24-shard-recovery/` |
| 相邻 sizing 测试 | `python -m pytest tests/test_stage23_formal_orchestration.py -k "sizing or reference_zero_norm" ... -q` | 2 passed，16 deselected，exit 0 | `.agent-temp/pytest/s24-shard-recovery-adj/` |
| G2.3 sizing/reference 测试 | `python -m pytest tests/test_stage2_g23_real_external_fixture.py tests/test_stage2_g23_evaluator.py -k "sizing or reference" ... -q` | 2 passed，17 deselected，exit 0 | `.agent-temp/pytest/s24-shard-recovery-g23/` |
| 语法检查 | `python -m py_compile src/param_importance_nlp/experiments/stage2_formal.py tests/test_stage23_formal_orchestration.py` | PASS，exit 0 | `.agent-temp/diagnostics/s24-shard-recovery/` |

### 证据身份与有效性

- producer/consumer commit：本地工作树基于 `141c9799fa1888eb7b487a3b3c76b8822ae67acf`，本轮尚未提交；未启动正式产物，故无新的 producer/consumer 证据。
- 既有 r19/staging/evidence 未修改；本轮只影响 sizing runner 的候选梯度提交边界，待主代理提交后只需重验该 sizing/recovery 单元，不因本地 HEAD 变化重跑 G3。
- 服务器只读核对：r19 输出在 `$DATA_ROOT/configs/generated/stage2/s204/prepared-r18-g3-v5/<cell>/resume/reference-sizing`；14M initialization/early 与 31M initialization 均有连续 `00000001..00000256`（B=8192）前缀，14M mid_late 有连续 `00000001..00000512`（B=16384）完整 ladder。四个最新 state 的 `plan_hash` 与 prepared-r18 sizing plan artifact hash 完全一致；registry hash 分别与 S2.3 qualification 的 `c0bd2a8e...ca04614`/`9724c41d...bf95afc` 一致；每个 state 的 `sizing_stream=true`、`shard_refs_b=[]`，没有 writer lock。
- 这些对象只证明 raw sizing prefix/ladder 的可复用身份，不把 BLOCKED attempt 升格为 formal evidence；B=8192 前缀可在原目录 append-only resume 至 B=16384，B=16384 ladder 不因 consumer 修复重算。服务器当前仍有旧代码进程（14M mid_late、31M initialization），未停止或覆盖。

### 失败与恢复

- 首个 blocker：`code=contract_unfrozen`，`requirement=stage2_reference_sizing`，`retryable=false`，消息为 `sizing shards do not cover every candidate B`。
- 根因类别：实现/编排边界；已排除 provider 性能、输入资产、GPU 与暂态连接原因。
- 安全恢复入口：修复后的 sizing runner 从既有 authoritative block commit 继续，或从新 attempt 运行完整预注册 ladder；不得直接跳过候选 B、重抽 A/B 或启动正式长跑前省略 targeted smoke。

### 下一步

- 主代理审阅本轮两文件 diff，形成独立代码提交后再决定服务器执行对齐；本轮不推送、不同步服务器、不启动 formal long run。
