# 2026-08-23 — S2.5 配对估计器运行器与公平预算

## 目标与范围

- 在冻结 S2.2 fixed-state provider 与 S2.3 draw/repetition mapping 身份之上，补齐 S2.5/G2.4a 的配对不变量、公平总样本预算、成本分层、不可变 shard/reducer、恢复和状态不变证据。
- 不读取或改写模型、数据集、服务器资产；不越过 provider/pure-estimator 接口，不升级依赖，不触碰根工作树 `presentation/` 三项用户修改。

## 实际修改

- `PairedEstimatorRunner`：所有 M 共用同一 `M_max` 基础梯度池；formal primary 只给 `M>2` 时仍自动计算 M=2 U 并与同预算 double 逐坐标校验；校验 draw/sample 顺序、参数 registry、有限梯度与 fixed-state digest。
- `PairedEstimatorResult`：显式记录 `sample_budget/sample_count/statistical_weight`、gradient/formula/wall-clock 与 peak-memory 成本字段；科学 digest 排除机器计时但绑定预算身份。
- `SufficientStatisticShard`/`DeterministicShardReducer`：向量统一复制为只读 NumPy、绑定 registry/state/预算/weighting 身份，按 canonical unit ID 确定性归并并暴露累计预算/成本；持久化 bundle/commit 加强 state schema、重复/漂移/损坏拒绝。
- `RecoverablePairedWaveRunner`：unit state 保存完整预算/成本字段，失败写入独立 append-only failure record，下一次按同一 mapping hash 重算；wave task payload 改用既有 `stage2-paired-wave-summary-v1` 并带 `artifact_hash`，补充 shard/unit/failure schema。
- 新增两项定向测试，覆盖 primary-only M 自动补 M=2、预算/成本、shard 只读和 reducer 汇总。

## 验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| S2.5 sampling/runner 定向 | `python -m pytest --basetemp .agent-temp/pytest/s205-final-targeted-01/basetemp -o cache_dir=.agent-temp/pytest/s205-final-targeted-01/cache tests/test_experiments_sampling_stage2.py -q` | 15 passed，exit 0 | `.agent-temp/pytest/s205-impl-04/`；最终组合同样复验 |
| S2.5 + formal orchestration + task runner 组合 | 同上并加入 `tests/test_stage23_formal_orchestration.py tests/test_stage23_task_runners.py` | 53 passed，exit 0，112.09s | `.agent-temp/pytest/s205-final-targeted-01/` |
| schema/handoff 定向 | `tests/test_artifact_schemas_and_loaders.py tests/test_stage2_s202_handoff_contract.py` | 23 passed，exit 0 | `.agent-temp/pytest/s205-impl-05/` |
| Python 编译与新增 JSON 解析 | `python -m compileall -q src tests`；PowerShell `ConvertFrom-Json` | compile exit 0；3 个新增 schema 均 json-ok | `.agent-temp/compile/s205-01/` |

## 证据身份与有效性

- producer commit：`9c6bb07c7461d5153c93829e682dbee3a20ae062`，基线 `ed173f10412b6c181d05aea57d75e29f676dadcd`。
- consumer commit：本 worklog 追加提交生成后填写；worklog 不改写 producer 的代码/测试身份。
- 输入身份：只消费 S2.2 fixed-state/provider contract 与 S2.3 frozen sampling/repetition mapping API；本地 fixture 测试不宣称 formal G2.4a 或 G2.3/G2.4b。
- 既有上游证据未因本次下游 runner/schema 变化失效；本提交仅影响 S2.5 runner 及其下游 wave artifact 消费。

## 失败与恢复

- 初次 targeted pytest 使用错误路径，未执行测试；随后在专用 `.agent-temp` 目录重跑通过。一次语法回归因 `stage2_formal.py` 静态方法缩进错误阻断收集，立即修复后定向组合 53 passed；未删除断言或跳过测试。
- 运行时失败记录写入 `resume/paired-wave/failures/<unit>-<attempt>.json`，不覆盖 shard commit；恢复复用冻结 mapping/checkpoint/provider state。

## Git 与同步

- 本机 worktree：`D:\Personal\Code\parameter-importance\.agent-temp\worktrees\stage2-s205`，分支 `feat/stage2-s205`；根工作树用户 presentation 修改保持原状。
- 当前仅需发布代码分支；未执行服务器运行或 execution alignment，未上传大型资产。

## 下一步

- 提交本 worklog consumer 后推送 `feat/stage2-s205`，由主代理审阅/合并；正式 pilot/matrix 的 G2.3/G2.4b 仍由 S2.6 负责。
