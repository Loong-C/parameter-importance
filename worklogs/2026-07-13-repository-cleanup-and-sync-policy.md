# 2026-07-13 仓库脚本清理与多端同步规范

## 本次目标

- 判断环境准备完成后 `scripts/` 中的文件是否仍被后续实验使用。
- 删除确认无运行期用途的一次性准备脚本，保持仓库整洁。
- 在 `Agents/` 下建立中文工作日志与多端 Git 同步规范。
- 将最终变更同步到本机、GitHub 和 sophgo13 服务器仓库。

## 审计结论

- `scripts/` 中共有 22 个受 Git 跟踪文件，全部在本次环境准备相关提交 `96ec8bf`、`fd6b69c` 和 `8222410` 中引入。
- 仓库现有实验计划、文档和代码没有继续引用这些脚本；引用只存在于已经完成的历史工作日志以及被 Git 忽略的连接说明中。
- sophgo13 的 `manifests/READY` 已于 `2026-07-13T12:17:39+00:00` 生成，服务器上使用仓库 `scripts/` 的进程数为 0。
- 因此这些文件都是 wheelhouse 构建、资产缓存、Pile 下载、服务器安装或最终离线验收的一次性工具，不属于后续参数重要性实验的运行代码。
- 删除不会破坏复现证据：依赖锁文件、计划书、工作日志和服务器大盘 manifests 均保留；如需完整重建，可从 Git 提交 `74ae22de31c6222bc636adde765fb80b0bcc6e85` 恢复原脚本。

## 已完成内容

### 删除的一次性文件

- lab-pc 下载、缓存、wheelhouse 构建、传输和监督脚本：9 个。
- 服务器安装、下载、核心验收和最终收尾脚本：7 个。
- Pile 前缀、官方 batch viewer、依赖锁、资产 manifest、离线模型数据和 NCCL 辅助验证程序：6 个。
- 同时清理 `scripts/__pycache__/` 中由本机语法检查产生的忽略文件；删除后仓库不再保留空的 `scripts/` 目录。

### 新增规范

- 新增 `Agents/工作日志与多端同步规范.md`，明确：
  - 每次工作日志必须记录的八类信息；
  - 敏感信息和大型产物的禁止提交范围；
  - 修改前状态检查、显式暂存、测试和提交要求；
  - GitHub 快进推送、网络失败恢复与禁止强推规则；
  - 使用经过验证的 Git bundle 将本机提交安全快进到 sophgo13；
  - 服务器产生修改时的回流方式；
  - 本机、GitHub、服务器三端一致的完成判定。
- `worklogs/README.md` 增加指向该完整规范的入口。
- `Agent/remote_access.md` 删除对现行仓库脚本的过时依赖，说明历史恢复提交。

## 验证与同步状态

- 已确认本机物理 `scripts/` 目录不存在，Git 中恰有 22 个待提交删除的原脚本文件，忽略的 `__pycache__` 也已清理。
- 已扫描计划书、文档、环境声明、新规范和 `worklogs/README.md`，指向 22 个已删除文件名的现行引用为 0；计划书推荐目录树已用 `Agents/` 与 `worklogs/` 替换旧 `scripts/` 项。
- `git diff --check` 通过；新增规范链接存在；签名参数、令牌参数和私钥头敏感模式扫描通过。
- 清理与规范提交 `60279ea1f32ebc123f2f64f568785c795a1cb4fc` 已推送 GitHub `main`；系统 DNS 路径多次出现 443 connect timeout，最终只在两个公共 DNS 共同返回且 5 秒 TCP 探测可达的地址中选择本次命令级绑定，未修改系统 DNS、`hosts` 或记录具体 IP。
- 服务器仓库在确认工作树干净后，通过 `git bundle verify` 通过的 bundle 执行 `git merge --ff-only FETCH_HEAD`，快进到同一提交；服务器已确认 `scripts/` 不存在、`Agents/工作日志与多端同步规范.md` 存在，临时 bundle 已按确切路径清理。
- 被 Git 忽略的 `Agent/remote_access.md` 已单独复制到服务器；本节所在的最终日志提交将再次推送并以相同 bundle 流程快进，随后比较本机、GitHub、服务器 HEAD 及本机/服务器日志 SHA-256。
