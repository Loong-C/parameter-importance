# 2026-08-04 Stage 0 重新执行（redo）

- 任务范围：重新执行 Stage 0；先重验 S0.1（G0-C/G0-G 基线），随后按 G1→G10 顺序逐项重跑并完成 S0.2–S0.12。
- 当前状态：S0.1（G0-C/G0-G/G0 PASS）、S0.2（G1 PASS）、S0.3（G2 PASS）、
  S0.4（G3 PASS）、S0.5（G4 PASS）、S0.6（G5 PASS）、S0.7（G6 PASS）
  均已完成；S0.8–S0.12 待执行。
- 工作分支：`feat/stage0-completion`

## 2026-08-04 14:03 CST — S0.1 重验：三端基线采集与 G0 判定

### 目标与范围

- 本阶段要完成什么：重新采集本机/GitHub/服务器仓库基线、`Agent/` 文档哈希、服务器系统/存储/GPU 健康状态，给出本轮 G0-C 与 G0-G 判定。
- 不在本阶段处理什么：不执行 S0.2–S0.12；不进行 GPU 复位/驱动/重启等管理员操作；不下载资产、不重建环境。

### 实际修改

- 代码、配置、文档：新增本报告与工作日志；无功能代码改动。
- 服务器或外部状态：只读复采，未做任何写入。
- 用户原有修改：无；快进更新后工作树干净，旧提交保留在新 HEAD 历史中。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 本机 Git 基线 | `status --short --branch`、`rev-parse HEAD`、`remote -v`、`log -1` | 分支 `redo/from-run-ready`、HEAD `8f1bc5b`、工作树干净 | 本日志；`reports/stage0/g0-baseline-redo-20260804.json` |
| GitHub 分支 HEAD | `git ls-remote origin` | `main=34966d0`、`feat/stage0-completion=8f1bc5b`；`redo/from-run-ready` 不存在 | 同上 |
| 服务器仓库基线 | SSH 只读 `git branch --show-current` 等 | `feat/stage0-completion` @ `1746903`，工作树干净，落后 GitHub 1 提交 | 同上 |
| Agent 文档哈希 | 本机 `Get-FileHash`、服务器 `sha256sum` | 5/5 文件 SHA-256 完全一致 | 同上 |
| 系统/存储基线 | `/etc/os-release`、`uname -r`、`nproc`、`free -g`、`df`、`stat` | Ubuntu 24.04.3 LTS；6.8.0-136；128 CPU；1007 GiB；大盘 2.8T 可用；`DATA_ROOT` 0750 | 同上 |
| GPU 健康复采 | `nvidia-smi -L`、query ECC/compute apps（只读） | 4 卡可见；GPU 3 ECC=N/A，存在 PID=N/A compute app；判定 DEGRADED | 同上 |

实验元数据（适用时）：

- Git 提交：本阶段报告将随提交绑定（见 Git 与多端同步）。
- 随机种子：不适用。
- 配置文件：不适用。
- 数据集及 revision：不适用。
- 模型及 revision：不适用。
- checkpoint：不适用。
- 设备/关键环境：Windows 本机采集；服务器 Ubuntu 24.04.3 LTS、内核 6.8.0-136-generic、NVIDIA 驱动 575.57.08（沿用 08-03 记录）。
- 结果目录：`reports/stage0/`。

### 产物与证据

| 路径 | 类型 | 大小 | SHA-256/revision | 验收状态 |
|---|---|---:|---|---|
| `reports/stage0/g0-baseline-redo-20260804.json` | 机器可读基线报告 | 随提交绑定 | Git blob/提交 SHA | 通过（本机解析） |
| `reports/stage0/g0-baseline-redo-20260804.md` | Markdown 摘要 | 随提交绑定 | Git blob/提交 SHA | 通过 |

### 问题、原因与风险

- 遇到的问题：GPU 3（`0000:A4:00.0`）健康状态仍异常。
- 已确认根因：08-04 内核记录 Xid 120/119/154，恢复动作 GPU Reset Required；当前 ECC 字段 N/A，存在 PID 不可见 compute app。
- 采取的恢复措施：只读复采并 fail-closed 记录 `BLOCKED`；不执行任何管理员动作。
- 未解决问题和风险：G0-G 需管理员恢复 GPU 3 后重验；当前分支 `redo/from-run-ready` 未推送 GitHub，多端同步待用户决策。

### Git 与多端同步

- 本机分支/HEAD：`redo/from-run-ready` / `8f1bc5b6bdaa1f7c875c246ee59e6acc7e37c763`。
- GitHub 分支/HEAD：`feat/stage0-completion` / `8f1bc5b6bdaa1f7c875c246ee59e6acc7e37c763`；`redo/from-run-ready` 不存在。
- 服务器分支/HEAD：`feat/stage0-completion` / `1746903c46dd90142644e6c3f2d529000b9c6131`（落后 1 提交）。
- `Agent/*.md` 哈希核对：本机与服务器 5/5 一致。
- 工作树状态和临时 bundle 清理：本机工作树干净；本轮未创建 bundle。

### 下一步

- 用户决策：确认当前分支是否推送 GitHub（或切换到已推送的 `feat/stage0-completion`）。
- 管理员动作：恢复 GPU 3（`0000:A4:00.0`）并给出处置结论。
- 恢复后动作：重跑 G0-G 四卡资格、逐卡 CUDA 张量与四卡 NCCL smoke，重新发布 G0-G 报告；随后进入 S0.2（G1-B/G1-D）重验。

## 2026-08-04 14:25 CST — 分支切换与三端同步更正

- 用户决定使用 `feat/stage0-completion` 作为工作分支，不使用 `redo/from-run-ready` 推送。
- 本机已切换到 `feat/stage0-completion` 并快进到 `b4a3259`；该提交已推送 GitHub，并经增量 bundle（前置 `1746903`，13,346 字节）快进到服务器；服务器 HEAD 与 GitHub 完全一致，工作树干净，临时 bundle 已按精确路径在两端删除。
- 免密 sudo 探测：`sudo -n true` 返回需要密码（`sudo: a password is required`）；Agent 不读取、不记录、不代输密码，管理员级 GPU 动作仍需用户在可见 TTY 输入密码执行。
- G0-G 判定不变：仍 `BLOCKED`（GPU 3 `0000:A4:00.0` 待管理员恢复）。
- 上一节“下一步”中的“用户决策”项已解决（分支确定为 `feat/stage0-completion`）；其余下一步不变。

## 2026-08-04 15:15 CST — S0.1 重验完成（G0-C/G0-G PASS）与 G2 重新晋级

### 目标与范围

- 完成 S0.1 重验：G0-C 与 G0-G 均重新取得当前状态证据；同时按 08-03 流程重新晋级 G2，恢复训练可用环境引用。
- 不在本阶段处理：S0.2–S0.12；真实资产下载与 formal 训练。

### 实际修改

- 代码、配置、文档：新增 `ops/stage0/run_cuda_nccl_smoke.py`（CUDA/NCCL smoke 证据生成器，含 4 次修复提交：torch 导入顺序、spawn worker、空闲基线等待、摘要读取）；新增本报告与日志；无其他功能改动。
- 服务器或外部状态：用户执行 `sudo reboot` 更换 boot；用户执行 service-finalizer 恢复服务并生成管理员证据；本 Agent 运行 CUDA/NCCL smoke 与 G0-G/G2 晋级器，发布新的 qualification 与推荐环境引用。
- 用户原有修改：无；工作树干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 重启后 GPU 健康 | `nvidia-smi` 四卡查询（只读） | 4/4 白名单卡 ECC=0、row-remap No、无计算进程 | `reports/stage0/g0-g-gpu-final-20260804.json` |
| 管理员 finalizer | `run_admin_gpu_service_finalize.ps1` 直接执行修复后脚本 | `G0_G_UUID_EXCLUSION_SERVICE_FINALIZE_PASS`；run `20260804T065558Z` | `/var/lib/parameter-importance/stage0/g0-g-uuid-exclusion/service-finalize/finalize-20260804T065558Z.CBnTsok6` |
| CUDA/NCCL smoke | `run_cuda_nccl_smoke.py`（冻结环境） | `PASS`：逐卡张量 4/4、四 rank all-reduce 3 次 min/max=10.0、前后快照干净 | `$DATA_ROOT/operations/stage0/g0-g-uuid-exclusion/cuda-nccl-smoke-20260804T071342Z` |
| G0-G/G2 晋级 | `promote_environment_after_gpu_gate.py` | `PASS`；qualification `gpuq-v1-9950e742…`；推荐环境 `training_eligible=true` | `$DATA_ROOT/manifests/environment-gpu-qualifications/…`、`environment-recommended.json` |
| 三端同步 | Git bundle `--ff-only` | 本机/GitHub/服务器均为 `2bd5ca9` | 本日志 |

实验元数据（适用时）：

- Git 提交：证据与生成器绑定 `2bd5ca9`（smoke 生成器最终提交）；报告随后提交。
- 随机种子：不适用。
- 配置文件：不适用。
- 数据集及 revision：不适用。
- 模型及 revision：不适用。
- checkpoint：不适用。
- 设备/关键环境：sophgo13，boot `1dc04123-4945-4d8b-abcb-27fb84725526`，内核 6.8.0-136-generic，驱动 575.57.08；PyTorch 2.12.1+cu126、CUDA 12.6、cuDNN 9.10.2、NCCL 2.29.3。
- 结果目录：`reports/stage0/`；服务器 `operations/stage0/g0-g-uuid-exclusion/`。

### 产物与证据

| 路径 | 类型 | 大小 | SHA-256/revision | 验收状态 |
|---|---|---:|---|---|
| `reports/stage0/g0-g-gpu-final-20260804.json` | G0-G 机器可读报告 | 随提交绑定 | Git blob/提交 SHA | 通过 |
| `reports/stage0/g2-environment-final-20260804.json` | G2 机器可读报告 | 随提交绑定 | Git blob/提交 SHA | 通过 |
| `$DATA_ROOT/manifests/environment-gpu-qualifications/gpuq-v1-9950e742….json` | GPU qualification | 5155+ | `703725f3bcd7…` | 通过 |
| `$DATA_ROOT/manifests/environment-recommended.json` | 推荐环境引用 | 1260 | `02c3e2612c77…` | 通过 |

### 问题、原因与风险

- 遇到的问题：Windows 包装脚本与工作树 CRLF 导致 finalizer 首次执行失败；smoke 生成器经历 4 次失败（torch 自建上下文、spawn 导入、瞬时利用率、摘要引用）。
- 已确认根因与修复：CRLF 行尾在服务器用 `tr -d '\r'` 修复并校验；生成器改为“编排进程不导入 torch + 子进程执行 CUDA 工作 + 等待空闲基线”后通过。
- 采取的恢复措施：失败残留证据目录已按精确路径清理；最终证据全部在当前 boot 上重新生成。
- 未解决问题和风险：两张原始故障卡仍为持久排除（未修复）；GPU 3 本次由重启恢复，但硬件根因未由厂商确认；G1-D 限时风险接受至 2026-08-18。

### Git 与多端同步

- 本机分支/HEAD：`feat/stage0-completion` / `2bd5ca9`（本报告随下一提交）。
- GitHub 分支/HEAD：`feat/stage0-completion` / `2bd5ca9`。
- 服务器分支/HEAD：`feat/stage0-completion` / `2bd5ca9`。
- `Agent/*.md` 哈希核对：本机与服务器 5/5 一致（本轮未修改）。
- 工作树状态和临时 bundle 清理：本机干净；本轮 bundle 已按精确路径删除。

### 下一步

- 进入 S0.2 重验：G1-B 存储 canary/容量重跑；G1-D 由用户确认限时风险接受继续有效。
- 之后按 G3→G10 顺序重跑 S0.4–S0.12。

## 2026-08-04 15:35 CST — S0.2 重验完成（G1-B/G1-D/G1 PASS）

### 目标与范围

- 本阶段要完成什么：按 `plan/stage0/02_storage_and_layout.md` 重验 S0.2；重跑
  G1-B 存储 canary、容量/根盘/inode 检查、生命周期与 Git 大文件守卫；复核
  G1-D 限时风险接受仍有效；形成 G1 总 gate。
- 不在本阶段处理什么：S0.3–S0.12；不下载资产、不重建环境、不执行 GPU 实验；
  不新增或扩展任何风险接受范围。

### 实际修改

- 代码、配置、文档：新增 `reports/stage0/g1-storage-canary-20260804.json`、
  `reports/stage0/g1-capacity-budget-20260804.json`、
  `reports/stage0/g1-storage-mechanism-20260804.json`（及 .md）；本日志追加；
  无功能代码改动。
- 服务器或外部状态：在 DATA_ROOT 的 13 个规定目录执行 canary（写入/读取/
  SHA-256/原子替换/精确清理，全部为随机唯一小文件）；两份原始证据 JSON 先在
  `$DATA_ROOT/tmp/` 生成，随后按精确路径移至 `$DATA_ROOT/reports/stage0/`；
  活动 Pile `.part` 与锁未动。
- 用户原有修改：无；本机与服务器工作树均干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 本机相关测试 | `pytest tests/test_atomic_storage.py tests/test_git_guard.py tests/test_lifecycle_capacity.py` | 23 passed，退出码 0 | 本日志 |
| 服务器相关测试 | 同上（冻结 venv，CPU） | 23 passed，退出码 0 | 本日志 |
| Git 大文件守卫 | `git-guard --repo .`（本机与服务器） | 均 PASS，退出码 0 | 本日志 |
| 13 目录 canary | `storage-check --require-writable --canary` | 13/13 PASS，0 失败、0 残留 | `reports/stage0/g1-storage-canary-20260804.json` |
| 容量/启动预算 | `storage-budget-check`（E=620 GB） | `ok=true`；大盘可用 2.74 TiB、根盘 37.2 GiB、inode 使用率 1% | `reports/stage0/g1-capacity-budget-20260804.json` |
| 三端 Git 基线 | `status`、`rev-parse`、`ls-remote`、SSH | 本机/GitHub/服务器均为 `3bb0f5a`，工作树干净 | 本日志 |
| Agent 文档哈希 | 本机 `Get-FileHash`、服务器 `sha256sum` | 5/5 一致 | 本日志 |

实验元数据（适用时）：

- Git 提交：证据绑定 `3bb0f5a`（本阶段报告随下一提交）。
- 随机种子：不适用。
- 配置文件：`policies/storage-lifecycle.json`。
- 数据集及 revision：不适用（未读取正式输入；活动 Pile 下载仅做存在性观察）。
- 模型及 revision：不适用。
- checkpoint：不适用。
- 设备/关键环境：本机 Windows CPU；服务器 sophgo13 CPU（未执行 GPU 实验）。
- 结果目录：`reports/stage0/`；服务器 `$DATA_ROOT/tmp/`。

### 产物与证据

| 路径 | 类型 | 大小 | SHA-256/revision | 验收状态 |
|---|---|---:|---|---|
| `reports/stage0/g1-storage-canary-20260804.json` | canary 证据 | 随提交绑定 | `ee24d9865a89…` | 通过 |
| `reports/stage0/g1-capacity-budget-20260804.json` | 容量预算证据 | 随提交绑定 | `56ddecd38b86…` | 通过 |
| `reports/stage0/g1-storage-mechanism-20260804.json` | G1-B/G1 报告 | 随提交绑定 | Git blob/提交 SHA | 通过 |
| `reports/stage0/g1-storage-mechanism-20260804.md` | Markdown 摘要 | 随提交绑定 | Git blob/提交 SHA | 通过 |
| `$DATA_ROOT/reports/stage0/g1-storage-canary-20260804.json` | 服务器原始证据 | 15 KiB 级 | `ee24d9865a89…` | 通过 |
| `$DATA_ROOT/reports/stage0/g1-capacity-budget-20260804.json` | 服务器原始证据 | 1 KiB 级 | `56ddecd38b86…` | 通过 |

### 问题、原因与风险

- 遇到的问题：PowerShell 管道向远程 `bash -s` 传入脚本时注入 CRLF，导致
  pytest 把行尾 `\r` 当作路径的一部分，报 “file or directory not found”。
- 已确认根因：多行脚本经管道传输后的行尾字节问题。
- 采取的恢复措施：远程脚本改为 base64 传输后执行；服务器 23 项测试全部通过，
  canary 与容量结果不受影响。
- 未解决问题和风险：G1-D 仍是无第二故障域的单盘限时风险接受（至
  2026-08-18 23:59 CST 或 Stage 4 开始前）；GPU 3 硬件根因尚未由厂商确认；
  进入 Stage 4 前必须重新建立持久性决策。

### Git 与多端同步

- 本机分支/HEAD：`feat/stage0-completion` / `3bb0f5a`（本阶段报告随下一提交）。
- GitHub 分支/HEAD：`feat/stage0-completion` / `3bb0f5a`。
- 服务器分支/HEAD：`feat/stage0-completion` / `3bb0f5a`。
- `Agent/*.md` 哈希核对：本机与服务器 5/5 一致。
- 工作树状态和临时 bundle 清理：本机工作树干净；本轮暂未创建 bundle。

### 下一步

- 提交本阶段报告与日志，推送 GitHub，并用已核验 bundle 快进同步服务器后清理。
- 之后按 G3→G10 顺序重跑 S0.4–S0.12（S0.3/G2 已于 15:15 条目重新晋级）。

## 2026-08-04 15:53 CST — S0.2/S0.3 完成状态审计与 S0.4 启动判定

### 目标与范围

- 按 `Agent/` 五份运维文档复核 S0.2（G1-B/G1-D/G1）与 S0.3（G2）是否完成，
  并判断 S0.4 是否满足启动前置。
- 不在本阶段处理：不执行 S0.4 下载、不修改 SSH 拓扑、不做管理员级动作。

### 实际修改

- 代码、配置、文档：仅追加本日志；无功能代码改动。
- 服务器或外部状态：只读探针 `lab-pc` 与 `sophgo13-via-lab`，未做任何写入。
- 用户原有修改：无；本机工作树干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 本机 Git 基线 | `status --short --branch`、`rev-parse HEAD`、`diff --check` | `feat/stage0-completion` @ `e430296`，工作树干净 | 本日志 |
| GitHub 分支 HEAD | `git ls-remote origin` | `feat/stage0-completion=e430296`，与本机一致 | 本日志 |
| S0.2/G1 报告 | 解析 `g1-storage-mechanism-20260804.json/.md` 及 canary/容量证据 | G1-B/G1-D/G1 均 `PASS`；13/13 canary、0 残留；G1-D 限时接受至 2026-08-18 | `reports/stage0/g1-*-20260804.*` |
| S0.3/G2 报告 | 解析 `g2-environment-final-20260804.json` | `COMPLETE`；G2 `PASS`、`training_eligible=true`；qualification `gpuq-v1-9950e742…` | `reports/stage0/g2-environment-final-20260804.json` |
| 远程通道 | `ssh -o BatchMode=yes lab-pc`、`sophgo13-via-lab`（有限超时） | `BLOCKED`：lab-pc banner 交换超时；sophgo13-via-lab 无法解析/超时，服务器状态无法复核 | 本日志 |
| Agent 文档哈希 | 本机 `Get-FileHash SHA256 Agent/*.md` | 本机 5/5 正常读取；服务器核对因隧道断开未执行 | 本日志 |

### 判定

- S0.2：完成。G1-B、G1-D、G1 均为 `PASS`，本机证据已提交并推送到 GitHub
  （`feat/stage0-completion` @ `e430296`）。
- S0.3：完成。G2 于 2026-08-04 15:15 在当前 boot 重新晋级为 `PASS`，
  训练可用环境引用已发布；证据随 `3bb0f5a` 提交。
- S0.4：启动前置（G0-C、G1，另有 G2）均满足，从 gate 角度可以开始；但
  S0.4/G3 本身仍是 `IN_PROGRESS`（缺 tokenizer 3 文件、MNLI 5 文件、RTE 3 文件、
  三个 GLUE derived、13 份 manifest/qualification 与 G3-S1/S2/S4/S5/S6 收口），
  且当前 SSH 反向隧道断开，服务器侧下载与同步无法执行。

### 问题、原因与风险

- 遇到的问题：`lab-pc` 与 `sophgo13-via-lab` 均不可达，服务器 HEAD 与
  `Agent/*.md` 哈希无法复核。
- 已确认根因：反向隧道当前断开（banner 交换超时），按 `Agent/remote_access.md`
  必须由 lab-pc 桌面会话启动 `start.cmd`，不得另建替代拓扑。
- 采取的恢复措施：只读探测并记录；未修改连接参数或尝试绕过。
- 未解决问题和风险：服务器落后本机/GitHub 三个提交（上次已知 `3bb0f5a`），
  隧道恢复后必须先快进服务器再开始 S0.4 服务器侧工作；G1-D 有效期不变。

### Git 与多端同步

- 本机分支/HEAD：`feat/stage0-completion` / `e430296`（本日志随下一提交）。
- GitHub 分支/HEAD：`feat/stage0-completion` / `e430296`。
- 服务器分支/HEAD：隧道断开，无法复核；上次已知为 `3bb0f5a`。
- `Agent/*.md` 哈希核对：本机正常；服务器复核待隧道恢复。
- 工作树状态和临时 bundle 清理：本机干净；未创建 bundle。

### 下一步

- 用户在 lab-pc 桌面会话确认/启动 `Lab SSH Tunnel`（`start.cmd`）恢复反向隧道。
- 隧道恢复后先只读复核服务器仓库、推荐环境、GPU qualification、活动下载与资产
  目录，再用已核验 bundle 把服务器快进到 `e430296` 并核对 `Agent/*.md`。
- 随后按冻结的 G3 合同继续 S0.4：补齐剩余对象、三个 GLUE derived、13 份
  manifest/qualification 与 G3-S1/S2/S4/S5/S6，全部通过后才进入 S0.5/G4。

## 2026-08-04 20:08 CST — S0.4/G3 完成

### 目标与范围

- 按 `plan/stage0/04_assets_and_manifests.md` 完成 S0.4：补齐缺失资产、
  构建三个 GLUE derived、发布 13 份 manifest/qualification、通过
  G3-S1/S2/S4/S5/S6 与 G3 总 gate，并输出 formal 证据。
- 不在本阶段处理：S0.5/G4 及之后任务。

### 实际修改（本机代码，已逐提交推送并快进服务器）

- `d4197da`：formal bootstrap 源报告引用切换到 08-04 当前 boot 的 G0-G/G2 报告。
- `178de6c`：`http-objects/*.json` 固定 LF 检出，保证 canonical 解析。
- `f5cb9ec`：G3 流 receiver 的不活动超时从 60s 提到 300s（高于 emitter 90s），
  避免慢链路下 receiver 先超时并在退出时崩溃。
- `9c442f0`：requirements/layout/download-plan 重新以 canonical JSON 发布；
  内容与 artifact hash 不变。
- `95126ff` + `c338df4`：GLUE derived 离线守卫放行回环连接与本地子进程
  （datasets 多进程 manager 所需），AF_UNIX 路径视为本地；外部 TCP/DNS 仍阻断。
- `f37e597`：保存后校验读取 state.json 的持久化 `_fingerprint`，兼容
  datasets 4.8.5 的 `load_from_disk` 重算行为。
- `05c4da6`：把 Pythia tokenizer 的 `vocab_mapping_sha256` 修正为冻结环境实际
  可复现值（`0056c519…`），并同步代码常量与控制面哈希。

### 服务器或外部状态

- 经 `local-via-lab`（本机直连 hf-mirror + SSH 管道）补齐 MNLI 5 个与 RTE 3 个
  parquet；RTE validation 此前已通过 lab-pipe 完成。所有 13 个对象最终文件与
  冻结大小/SHA-256 一致。
- 生成 canonical acquisition 报告、attestation（DOWNLOADED + 三个 GLUE derived）、
  独立 verify（VERIFIED）、gate-only materialize（13 READY + 13 qualification +
  resolution）、formal G3（S0.4 正式任务与 G3 各子 gate）。
- 清理本轮失败链中间产物：3 个 `tmp/glue-derived-*` staging 目录、10 个旧控制面
  下发布的 READY/qualification 中间文件；旧候选/报告保留为不可变历史。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 对象获取 | `dispatch_g3_relay_via_lab.py --relay-process local` | 8/8 对象完成，文件大小/哈希匹配 | `$DATA_ROOT/datasets/glue-{mnli,rte}` |
| 本机相关回归 | 控制面/守卫/指纹/formal 相关测试 | 多轮共 468 passed，0 failed（含 221 项 gate 套件、85 项收尾套件） | 本日志 |
| G3 acquisition | `acquire_g3_missing_assets.py` | `PASS objects=13` | `operations/stage0/g3-download-05c4da6.json` |
| G3 attestation | `attest_g3_materialization.py` | `published assets=13` | `manifests/evidence/g3/acquisition/72f73620….json` |
| G3 verify-only | `verify_g3_assets.py` | `published assets=13` | `manifests/evidence/g3/verification/72f73620….json` |
| G3 materialize | `materialize_and_publish_g3.py` | `PASS assets=13`；resolution `6c7f6ecf…` | `reports/stage0/g3/6c7f6ecf…/asset-index.json` |
| formal G3 | `formalize_g3.py` | `PASS`；G3-S1/S2/S4/S5/S6 与 G3 全部 PASS | `evidence/stage0/g3-formal/6c7f6ecf…/index.json` |

### 产物与证据

- `$DATA_ROOT/manifests/model/*.json`（5 份）、`manifests/tokenizer/pythia.json`、
  `manifests/data/*.json`（7 份）：全部 `ready`。
- `$DATA_ROOT/manifests/qualifications/*.json`：13 份 qualification。
- `$DATA_ROOT/reports/stage0/g3/6c7f6ecf…/{asset-index,asset-audit,asset-resolution}.json`。
- `$DATA_ROOT/evidence/stage0/g3-formal/6c7f6ecf…/{index,environment,resolved-config}.json`。
- 服务器侧完整文件集合/哈希由 acquisition 与 verify 报告绑定。

### 问题、原因与风险

- 已修复并记录的问题：Windows 检出 CRLF 破坏 canonical JSON；慢链路下 receiver
  超时崩溃；datasets 多进程被离线守卫误拦；`load_from_disk` 重算指纹；
  tokenizer vocab 映射哈希冻结值错误；失败链残留 READY 阻塞重发布。
- 未解决问题和风险：G1-D 单盘风险接受仍至 2026-08-18 23:59 CST 或 Stage 4 前；
  GPU 硬件根因未由厂商确认；正式训练尚未开始。

### Git 与多端同步

- 本机分支/HEAD：`feat/stage0-completion` / `05c4da6`（本日志随下一提交）。
- GitHub 分支/HEAD：`feat/stage0-completion` / `05c4da6`。
- 服务器分支/HEAD：`feat/stage0-completion` / `05c4da6`，工作树干净。
- `Agent/*.md` 哈希核对：本机与服务器 5/5 一致。
- 工作树状态和临时 bundle 清理：本机干净；各轮 bundle 均已在两端精确删除。

### 下一步

- 进入 S0.5/G4（配置、实验编号、seed 与 provenance）。

## 2026-08-04 20:54 CST — S0.4 完成复核与 S0.5 启动判定

### 目标与范围

- 复核 S0.4/G3 的完成证据，确认是否满足 S0.5/G4 启动前置。
- 不在本阶段处理：不开始 S0.5 正式执行，不做系统级管理员动作。

### 实际修改

- 本机与服务器按精确路径清理 4 个过期 bundle：本机
  `.sync-1b88e8d.bundle`；服务器
  `$DATA_ROOT/tmp/repo-sync-1b88e8d.bundle`、
  `server-preserve-fc5a4fc.bundle`、
  `server-preserve-fc5a4fc-incremental.bundle`。
- 追加本日志；无功能代码改动。

### 实验与验证

| 项目 | 结果 | 证据路径 |
|---|---|---|
| 本机 Git 基线 | `feat/stage0-completion` @ `c99081d`，工作树干净 | 本日志 |
| GitHub 分支 | `git fetch --prune` 后 `origin/feat/stage0-completion` = `c99081d` | 本日志 |
| 服务器 Git 基线 | `feat/stage0-completion` @ `c99081d`，工作树干净 | 本日志 |
| Agent 文档哈希 | 本机/服务器 5/5 SHA-256 一致 | 本日志 |
| G3 正式证据 | `gate_g3` 与 G3-S1/S2/S4/S5/S6 对象均 `status=PASS`，checked_at 2026-08-04T11:31:00Z | `$DATA_ROOT/evidence/stage0/g3-formal/6c7f6ecf25b6f9e8b0396c3438402345077ed9ef0ecb20c558980353778bc9da/` |
| 资产 qualification | 13/13 `status=PASS`；13 份 manifest 存在且 `state=ready`（含 downloading→downloaded→verified→ready 链） | `$DATA_ROOT/manifests/{model,tokenizer,data,qualifications}` |
| 本地回归 | G3 相关快速套件 246 passed/1 skipped（Windows symlink 能力跳过）；G4 邻近契约套件 58 passed | 本日志 |

### 判定

- S0.4：完成。G3 总 gate 及各子 gate 均为 PASS；资产 ID/schema 已冻结
  （requirements `status=FROZEN`）；formal 索引 `next_task_id=stage0.05_config_run_identity_and_seeds`。
- S0.5：可以开始。G0-C、G1、G2、G3 均为 PASS；下一步在服务器同一 HEAD 上
  以 G3 resolution `6c7f6ecf…` 为输入运行 `formalize_g4.py` 形成 G4 证据。

### Git 与多端同步

- 本机/GitHub/服务器将在本日志提交后再次核对并快进。
- 遗留 bundle 已在两端清理；服务器 `$DATA_ROOT/tmp/*.bundle` 为空。

### 下一步

- 提交本日志、推送 GitHub、用已验证 bundle 快进服务器并核对三端 HEAD 与
  `Agent/*.md` 哈希。
- 随后执行 S0.5/G4：`formalize_g4.py`（绑定 G3 resolution `6c7f6ecf…`）。

## 2026-08-04 21:09 CST — S0.5/G4 完成（配置、实验编号、seed 与 provenance）

### 目标与范围

- 按 `plan/stage0/05_config_run_identity_and_seeds.md` 完成 S0.5：正式运行 G4，
  发布 resolved config、run/attempt/session 身份、seed 计划与 provenance 证据，
  形成 G4 正式索引。
- 不在本阶段处理：S0.6 及之后任务；不修改配置/代码；不做系统级管理员动作。

### 实际修改

- 代码、配置、文档：无功能代码改动；追加本日志并更新顶部“当前状态”行。
- 服务器或外部状态：在服务器 `05c4da6` 创建并保留临时运行分支
  `formal/g4-run-05c4da6`（本地分支，不推送 GitHub），以 G3 formal index
  `6c7f6ecf…` 为输入运行 `formalize_g4.py`，生成 G4 证据；运行后切回
  `feat/stage0-completion` @ `2d2d460`。
- 用户原有修改：无；服务器与本机工作树均干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 正式 G4 运行 | `formalize_g4.py --generator-git-commit 05c4da6 --g3-index-ref evidence/stage0/g3-formal/6c7f6ecf…/index.json`（冻结 venv `parameter-importance-stage0-1bd963c65f75`） | `status=PASS`，exit 0；index artifact_hash `52f6ee42…` | `$DATA_ROOT/evidence/stage0/g4-formal/6c7f6ecf…/index.json` |
| G4 重放校验 | `load_stage0_g4_formal_state`（expected commit 05c4da6） | `REPLAY_PASS`；index_sha256 `3fb4d52e…`；gate_artifact_hash `72817edb…` | 本日志 |
| 输出产物 | resolved_config / run_identity / seed_plan / provenance_record 四类 commit | 全部 `formal_eligible=true`；config_hash `c7dfe2e3…` | `$DATA_ROOT/evidence/stage0/tasks/05-6c7f6ecf…/commits/` |
| G4 环境 | G4 environment.json | `passed_gate_ids` 含 `stage0.G4`；`next_task_id=stage0.06_single_gpu_smoke` | `$DATA_ROOT/evidence/stage0/g4-formal/6c7f6ecf…/environment.json` |
| 本机回归 | G4 邻近契约套件（config/identity/seed/json/bootstrap 等） | 58 passed，0 failed | 本日志 |

### 判定

- S0.5：完成，G4 = `PASS`。resolved config 摘要 `c7dfe2e3…`；seed 计划与
  run identity 已发布；provenance 记录 `git_commit=05c4da6`（G3/G4 同一生成提交，
  其后提交均为文档变更）。
- 下一步：S0.6 单卡 smoke（G5），前置 G0-G/G1/G2/G3/G4 均 PASS。

### Git 与多端同步

- 本机/GitHub/服务器：`feat/stage0-completion` @ `2d2d460`（本日志提交后再次快进核对）。
- 服务器保留 `formal/g4-run-05c4da6` 运行分支作为 G4 证据锚点，不推送 GitHub。
- `Agent/*.md` 哈希：本机/服务器 5/5 一致（本轮未修改）。
- 临时 bundle：本轮运行未创建；上轮遗留已清理。

### 下一步

- 提交本日志、推送 GitHub、用已验证 bundle 快进服务器并核对三端 HEAD 与
  `Agent/*.md` 哈希。
- 随后执行 S0.6/G5：`formalize_g5.py`（绑定 G4 index `6c7f6ecf…`）。

## 2026-08-05 04:50 CST — S0.6/G5 完成（单卡训练 smoke）

### 目标与范围

- 按 `plan/stage0/06_single_gpu_smoke.md` 完成 S0.6：FP32 确定性、极小过拟合、
  Pythia-14M BF16、显存稳定性与五类失败路径，形成 G5 单卡 gate。
- 不在本阶段处理：S0.7 及之后任务；不做系统级管理员动作。

### 实际修改（本轮发现并修复的正式链缺陷，均逐提交推送/同步并重放 G3→G5）

- `2b7e66f`：G4 formal config 的模型/tokenizer/数据 asset ID 必须使用逻辑名
  （G5 运行时 G3_RUNTIME_LOGICAL_ASSET_UNKNOWN）。
- `8970f6c`：G5 数据路由改为 Stage 0 冻结的 `debug` 区间 +
  `without_replacement_frozen_epoch`。
- `45140ca`：训练 decision hash 改为读取 `task.formal_eligibility`。
- `ee6906a`：estimator decision 约束仅对目录中声明消费 decision 的任务生效，
  Stage 0/1 不再被 Stage 2 决策错误阻塞。
- `2e42818`：训练引擎把每个 microbatch 显式搬运到参数所在设备。
- `e6d6863`：Stage 0 训练禁用重要性统计（单 microbatch 不满足 U 估计器）。
- `1468157`：tensor bundle 支持 0 维标量张量（optimizer step 状态）。
- `dec29c9` + `0775dcf`：G5 边界元数据递归转 JSON 安全，并兼容
  `freeze_json_mapping` 的 `mappingproxy`。
- 每轮修复后均在服务器正式运行分支重新执行 bootstrap → attest → verify →
  materialize → formalize_g3 → formalize_g4 → formalize_g5；被取代的
  manifest/qualification/derived 均先精确移入 `$DATA_ROOT/tmp/g3-pre-*` 备份。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 正式 G5 套件 | `formalize_g5.py --generator-git-commit 0775dcf --g4-index-ref evidence/stage0/g4-formal/6c783a93…/index.json` | `status=PASS`，14 个子进程；GPU `GPU-5c672d04-4f83-3cc0-80d0-0108b1b63267`；结束后无残留进程 | `$DATA_ROOT/evidence/stage0/g5-formal/aad89ea6…/index.json` |
| G5 重放校验 | `load_stage0_g5_formal_state`（expected commit 0775dcf） | `REPLAY_PASS`；index_sha256 `cc07cc86…`；gate_artifact_hash `d20089fb…` | 本日志 |
| gate 指标 | training_smoke_result 对象 | `gate_record.status=PASS`；fp32 max abs error `0.0`；overfit 3 次、memory 3 次、failure 5 类全部通过；validation `PASS` | `$DATA_ROOT/evidence/stage0/tasks/06-aad89ea6…/objects/training_smoke_result/70f0d2a5…json` |
| G5 环境 | G5 environment.json | `passed_gate_ids` 含 `stage0.G5`；`next_task_id=stage0.07_ddp_and_gradient_semantics` | `$DATA_ROOT/evidence/stage0/g5-formal/aad89ea6…/environment.json` |
| 本机回归 | tensor bundle/decision hash/运行时训练/G4 链等定向测试 | 多轮通过（含标量与 mappingproxy 回归） | 本日志 |

### 判定

- S0.6：完成，G5 = `PASS`。resolved config_hash `507a9da3…`；
  G5 正式索引绑定生成提交 `0775dcf`（G3/G4/G5 同一生成提交）。
- 下一步：S0.7/G6 四卡 DDP 与梯度语义，前置 G0-G/G5 均 PASS。

### Git 与多端同步

- 本机/GitHub/服务器：`feat/stage0-completion` @ `0775dcf`（本日志提交后再次快进核对）。
- 服务器保留 `formal/run-0775dcf` 运行分支作为 G5 证据锚点；更早的
  `formal/run-*` 分支保留为历史证据锚点，均不推送 GitHub。
- `Agent/*.md` 哈希：本机/服务器 5/5 一致（本轮未修改）。
- 临时 bundle：每轮同步后均已按精确路径清理。

### 下一步

- 提交本日志、推送 GitHub、用已验证 bundle 快进服务器并核对三端 HEAD 与
  `Agent/*.md` 哈希。
- 随后执行 S0.7/G6：`formalize_g6.py`（绑定 G5 index `aad89ea6…`）。

## 2026-08-05 05:20 CST — S0.6 完成复核与 S0.7 启动判定

### 目标与范围

- 复核 S0.6/G5 的正式证据与多端同步状态，判断 S0.7/G6 是否满足启动前置。
- 不在本阶段处理：不启动 S0.7 正式运行；不修改功能代码；不做管理员级动作。

### 实际修改

- 代码、配置、文档：仅追加本日志；无功能代码改动。
- 服务器或外部状态：只读复核 G5 正式索引、环境、gate 产物、G4 设备映射、
  GPU 快照与 Git 状态；未做任何写入。
- 用户原有修改：无；本机与服务器工作树均干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 本机 Git 基线 | `status --short --branch`、`rev-parse HEAD` | `feat/stage0-completion` @ `48a2674`，工作树干净 | 本日志 |
| GitHub 分支 HEAD | `git ls-remote origin` | `feat/stage0-completion = 48a2674`，与本机一致 | 本日志 |
| 服务器 Git 基线 | SSH 只读 `git status`、`branch`、`rev-parse` | `feat/stage0-completion` @ `48a2674`，工作树干净 | 本日志 |
| Agent 文档哈希 | 本机 `Get-FileHash SHA256`、服务器 `sha256sum` | 5/5 文件哈希完全一致 | 本日志 |
| G5 正式索引 | 冻结 venv 读取并重算 `artifact_hash` | `G5_INDEX_HASH_OK`；`next_task_id=stage0.07_ddp_and_gradient_semantics` | `$DATA_ROOT/evidence/stage0/g5-formal/aad89ea6…/index.json` |
| G5 gate 产物 | 跟随 `gate_ref` 读取对象 | `status=PASS`；`gate_record.status=PASS`；checked_at `2026-08-04T20:49:11Z`；18 项证据 | `$DATA_ROOT/evidence/stage0/tasks/06-aad89ea6…/objects/training_smoke_result/70f0d2a5….json` |
| G5 环境 | 读取 environment.json | `passed_gate_ids` 含 G0-C/G0-G/G1/G2/G3(含子 gate)/G4/G5；`next_task_id=stage0.07_ddp_and_gradient_semantics` | `$DATA_ROOT/evidence/stage0/g5-formal/aad89ea6…/environment.json` |
| G4 设备映射 | 跟随 `g4_provenance` 引用读取 | 4 个 UUID 与当前 `nvidia-smi` 四张白名单卡完全一致 | `$DATA_ROOT/evidence/stage0/tasks/05-6c783a93…/objects/provenance_record/…json` |
| GPU 快照 | `nvidia-smi`（只读） | 4/4 卡空闲（0 MiB/0%）、无计算进程、uncorrected ECC=0；GPU 3 corrected volatile=4（非阻塞，记录观察） | 本日志 |
| G5 replay 说明 | `load_stage0_g5_formal_state` | 在 `0775dcf` 正式锚点分支可重放；当前 `48a2674` 主分支按设计要求 HEAD 匹配，正式运行需在锚点分支执行 | 本日志 |

### 判定

- S0.6：完成。G5 正式索引、gate 产物与环境均 `PASS`，三端 Git 同提交
  `48a2674`，`Agent/*.md` 5/5 一致。
- S0.7：可以开始。启动前置 G0-G 与 G5 均为 `PASS`；四卡列表由 G4
  provenance 提供且与当前白名单一致，四卡当前空闲且 uncorrected ECC=0；
  `formalize_g6.py`、`run_g6_worker.py` 与 `stage0_g6` 正式链代码齐备。
- 注意事项：正式运行需在服务器 `formal/run-0775dcf` 锚点分支（HEAD
  `0775dcf`）上执行，`--generator-git-commit 0775dcf`、
  `--g5-index-ref evidence/stage0/g5-formal/aad89ea6…/index.json`；
  GPU 3 的 4 次 corrected volatile ECC 记入观察项，若运行中出现新增
  uncorrected ECC 或 Xid 事件则立即中止并按 G0-G 流程处理。

### Git 与多端同步

- 本机/GitHub/服务器：`feat/stage0-completion` @ `48a2674`（本日志提交后再次快进核对）。
- 服务器保留 `formal/run-0775dcf` 运行分支作为 G5/G6 证据锚点，不推送 GitHub。
- `Agent/*.md` 哈希：本机/服务器 5/5 一致（本轮未修改）。
- 临时 bundle：本轮仅追加日志，未创建。

### 下一步

- 提交本日志、推送 GitHub、用已验证 bundle 快进服务器并核对三端 HEAD 与
  `Agent/*.md` 哈希。
- 用户确认后在服务器锚点分支执行 S0.7/G6：`formalize_g6.py`（绑定 G5 index
  `aad89ea6…`）。

## 2026-08-05 08:15 CST — S0.7/G6 完成（四卡 DDP 与梯度语义）

### 目标与范围

- 按 `plan/stage0/07_ddp_and_gradient_semantics.md` 完成 S0.7：四卡 NCCL
  collective、数据分片、DDP/梯度累积/`no_sync` FP32 语义、BF16 功能路径、
  受控故障退出与无残留进程，形成 G6 分布式语义 gate。
- 不在本阶段处理：S0.8 及之后任务；不做系统级管理员动作。

### 实际修改（正式链缺陷修复，均逐提交推送/同步并在服务器正式分支重放 G3→G6）

- `74d297d`：CUDA Event 计时替代 `perf_counter`，降低小消息计时抖动；故障注入
  改为“失败标志 all-reduce + 全部 rank 快速退出”，避免其余 rank 挂起 barrier。
- `be59273`：按用户批准修订测量协议 v2：20 warmup、50 个测量样本、每样本取连续
  3 次 collective 的 CUDA Event 中位延迟（每规模 150 次），新增
  `schemas/stage0-g6-worker-plan-v2.json` 并同步计划文档。
- `5513c41`、`eed7064`、`b98ad73`、`1bec2f7`：诊断并迭代小消息双峰问题；最终
  协议固定消息规模 `[65536, 262144, 4194304]`（256 KiB/1 MiB/16 MiB），实测
  256 KiB 档三重复 CV≈2.6%、p95/median≤1.03。
- `f7566da`：ECC drift 判定只按 uncorrected 计数器（与 G0-G 合同一致），
  corrected volatile ECC 仅作诊断记录。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 正式 G6 套件 | `formalize_g6.py --generator-git-commit f7566da --g5-index-ref evidence/stage0/g5-formal/8d4c1594…/index.json` | `CHAIN_STATUS=PASS`；collective×3、semantic、recovery 全部 PASS；failure-rank rc=1、timed_out=False、7.0s；无残留进程 | `$DATA_ROOT/evidence/stage0/g6-formal/d9cc1005…/index.json` |
| G6 replay | `load_stage0_g6_formal_state`（expected commit f7566da） | `G6_REPLAY_OK`；index_sha256 `62f2572f…`；gate_artifact_hash `285023ce…` | 本日志 |
| gate 记录 | 读取 gate 产物 | `status=PASS`；checked_at `2026-08-05T08:01:19Z`；15 项证据 | `$DATA_ROOT/evidence/stage0/tasks/07-d9cc1005…/objects/distributed_validation/…json` |
| G6 环境 | environment.json | `passed_gate_ids` 含 G0-C…G6；`next_task_id=stage0.08_logging_and_tracking` | `$DATA_ROOT/evidence/stage0/g6-formal/d9cc1005…/environment.json` |
| collective 指标 | 三次重复报告 | 256 KiB：median 236.5/237.6/249.9 µs、p95≤1.026×；1 MiB、16 MiB 均达标 | `$DATA_ROOT/evidence/stage0/g6-suite/259eb084…/reports/collective-*.json` |
| 设备审计 | device-audit.json | 前后 uncorrected ECC=0；GPU 3 corrected 46→51（诊断项）；拓扑哈希不变 | 同上 `device-audit.json` |
| 本机回归 | `pytest tests/test_stage0_g6.py` | 3 passed | 本日志 |

### 判定

- S0.7：完成，G6 = `PASS`。G6 正式索引绑定生成提交 `f7566da`；前置 G0-G/G5
  均 PASS；三端 Git 同步、`Agent/*.md` 5/5 一致。
- 测量协议修订已同步到计划文档：`stage0-unified-measurement-v2`（20 warmup /
  50 样本 / 每样本 3 次中位数 / 256 KiB-16 MiB），未放宽任何阈值。

### 问题、原因与风险

- 已修复并记录的问题：comm hook 注解在 `from __future__ import annotations` 下
  被字符串化导致 PyTorch 校验失败（最终取消 bucket 注解）；`perf_counter` 小消息
  计时 CV 超限；4 B–64 KiB 消息在该 PCIe 拓扑存在双峰延迟（实测 68 µs/118 µs
  两档），无法在 20% CV 内稳定测量，故协议将最小消息固定为 256 KiB；故障注入
  原实现使其余 rank 挂起 barrier 直到 60 s 超时；运行中 GPU 3 corrected volatile
  ECC 会增长（46→51），按 G0-G 合同不阻断，但已记入诊断。
- 未解决问题和风险：GPU 3 corrected ECC 持续小幅增长（uncorrected=0），硬件根因
  未由厂商确认；G1-D 单盘风险接受仍至 2026-08-18 23:59 CST 或 Stage 4 前。

### Git 与多端同步

- 本机/GitHub/服务器：`feat/stage0-completion` @ `f7566da`（本日志提交后再次快进核对）。
- 服务器保留 `formal/run-f7566da` 运行分支作为 G6 证据锚点；更早 `formal/run-*`
  分支保留为历史证据锚点，均不推送 GitHub。
- `Agent/*.md` 哈希：本机/服务器 5/5 一致（本轮未修改）。
- 临时 bundle：每轮同步后均已按精确路径清理；诊断用快速基准脚本与链日志保留在
  `$DATA_ROOT/tmp/` 作为证据。

### 下一步

- 提交本日志、推送 GitHub、用已验证 bundle 快进服务器并核对三端 HEAD 与
  `Agent/*.md` 哈希。
- 随后执行 S0.8/G7：`formalize_g7.py`（绑定 G6 index `d9cc1005…`）。

## 2026-08-05 16:20 CST — S0.7 完成复核与 S0.8 启动判定

### 目标与范围

- 复核 S0.7/G6 的正式证据与多端同步状态，判断 S0.8/G7 是否满足启动前置。
- 不在本阶段处理：不启动 S0.8 正式运行；不修改功能代码；不做管理员级动作。

### 实际修改

- 代码、配置、文档：仅追加本日志；无功能代码改动。
- 服务器或外部状态：只读复核 G6 正式索引、环境、gate 产物、GPU 快照与 Git 状态；
  在锚点分支 `formal/run-f7566da` 执行 G6 重放后切回主分支，未做任何写入。
- 用户原有修改：无；本机与服务器工作树均干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 本机 Git 基线 | `status --short --branch`、`rev-parse HEAD` | `feat/stage0-completion` @ `0460be9`，工作树干净 | 本日志 |
| GitHub 分支 HEAD | `git ls-remote origin` | `feat/stage0-completion = 0460be9`，与本机一致 | 本日志 |
| 服务器 Git 基线 | SSH 只读 `git status`、`branch`、`rev-parse` | `feat/stage0-completion` @ `0460be9`，工作树干净 | 本日志 |
| Agent 文档哈希 | 本机 `Get-FileHash SHA256`、服务器 `sha256sum` | 5/5 文件哈希完全一致 | 本日志 |
| G6 正式索引 | 冻结 venv 读取并重算 `artifact_hash` | `G6_INDEX_HASH_OK`；`next_task_id=stage0.08_logging_and_tracking` | `$DATA_ROOT/evidence/stage0/g6-formal/d9cc1005…/index.json` |
| G6 gate 产物 | 跟随 `gate_ref` 读取对象 | `status=PASS`；`gate_record.status=PASS`；`checked_at 2026-08-05T08:01:19Z`；world_size=4；residual_process_count=0；4 张 GPU UUID 与当前快照一致 | `$DATA_ROOT/evidence/stage0/tasks/07-d9cc1005…/objects/distributed_validation/…` |
| G6 环境 | environment.json | `passed_gate_ids` 含 G0-C…G6；`next_task_id=stage0.08_logging_and_tracking` | `$DATA_ROOT/evidence/stage0/g6-formal/d9cc1005…/environment.json` |
| G6 replay | `load_stage0_g6_formal_state`（expected commit f7566da，锚点分支 `formal/run-f7566da`） | `G6_REPLAY_OK`；index_sha256 `62f2572f…`；gate_artifact_hash `285023ce…` | 本日志 |
| GPU 快照 | `nvidia-smi`（只读） | 4/4 卡空闲（0 MiB/0%）；无计算进程；uncorrected ECC=0；GPU 3 corrected volatile=51（非阻塞，诊断项） | 本日志 |
| 本机回归 | `pytest tests/test_stage0_g6.py` | 3 passed | 本日志 |
| S0.8 代码就绪 | `formalize_g7.py`、`run_g7_worker.py`、`stage0_g7.py`、`stage0_g7_worker.py`、g7 schemas | 已存在于当前 HEAD | 本日志 |

### 判定

- S0.7：完成。G6 正式索引、gate 产物与环境均为 PASS；锚点分支重放通过；三端 Git
  同提交 `0460be9`；`Agent/*.md` 5/5 一致。
- S0.8：可以开始。启动前置 G1、G4 与 G6 均 PASS；`next_task_id=stage0.08_logging_and_tracking`；
  `formalize_g7.py` 等正式链代码已就绪。
- 注意事项：正式运行需在服务器锚点分支 `formal/run-f7566da`（HEAD `f7566da`）上执行，
  使用 `--generator-git-commit f7566da`、`--g6-index-ref evidence/stage0/g6-formal/d9cc1005…/index.json`；
  GPU 3 的 51 次 corrected volatile ECC 仅作观察项，若运行中出现新增 uncorrected ECC 或
  Xid 事件则立即中止并按 G0-G 流程处理。

### Git 与多端同步

- 本机/GitHub/服务器：`feat/stage0-completion` @ `0460be9`（本日志提交后再核对）。
- 服务器保留 `formal/run-f7566da` 运行分支作为 G6 证据锚点，不推送 GitHub。
- `Agent/*.md` 哈希：本机与服务器 5/5 一致（本轮未修改）。
- 临时 bundle：本轮同步后按精确路径清理。

### 下一步

- 提交本日志、推送 GitHub、用已验证 bundle 快进服务器并核对三端 HEAD 与
  `Agent/*.md` 哈希。
- 用户确认后在服务器锚点分支执行 S0.8/G7：`formalize_g7.py`（绑定 G6 index `d9cc1005…`）。

## 2026-08-05 16:32 CST — S0.8/G7-LOGGING 完成（日志、追踪与运行状态）

### 目标与范围

- 按 `plan/stage0/08_logging_and_tracking.md` 完成 S0.8：事件 schema、rank/全局 JSONL、
  状态机/心跳、canonical lineage、TensorBoard 派生、敏感信息与体积保护、追踪开销成对
  测量，形成 `stage0.G7-LOGGING` 组件 gate。
- 不在本阶段处理：S0.9 恢复场景最终验收（`full_g7_status=PENDING_S0.9`）；不做系统级
  管理员动作。

### 实际修改

- 代码、配置、文档：无功能代码改动（首次正式运行即通过）；仅追加本日志。
- 服务器或外部状态：在锚点分支 `formal/run-f7566da`（HEAD `f7566da`）用冻结 venv 运行
  `formalize_g7.py`；生成 `g7-suite` 与 `g7-formal` 证据；正式运行后服务器切回
  `feat/stage0-completion`。
- 用户原有修改：无；本机与服务器工作树均干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 正式 G7 运行 | `formalize_g7.py --repository … --data-root … --generator-git-commit f7566da --g6-index-ref evidence/stage0/g6-formal/d9cc1005…/index.json` | `status=PASS`（退出 0），约 30 秒完成；`checked_at 2026-08-05T08:28:27Z`；config_hash `a80bc899…`；environment_hash `fdde65b3…` | `$DATA_ROOT/evidence/stage0/g7-formal/285023ce…/index.json` |
| gate 记录 | 读取 gate 产物 | `stage0.G7-LOGGING` `status=PASS`；max_tracking_overhead_fraction `0.0629`（≤0.10）；paired_repetitions 3；canonical_optimizer_steps 4；sensitive_pattern_hits 0 | `$DATA_ROOT/evidence/stage0/tasks/08-285023ce…/objects/logging_overhead_report/…` |
| 开销检查 | 成对 worker 报告 | 最大成对开销 `0.0724`；formal CV `0.0062`、minimal CV `0.0006`；6/6 worker PASS | `$DATA_ROOT/evidence/stage0/g7-suite/a80bc899…/reports/g7-*.json` |
| 功能套件 | functional-report | JSONL 真值、rank 隔离、恢复 lineage、状态转换、TensorBoard 失败保真全部 PASS | `$DATA_ROOT/evidence/stage0/g7-suite/a80bc899…/functional/functional-report.json` |
| G7 replay | `load_stage0_g7_formal_state`（expected commit f7566da，锚点分支） | `G7_REPLAY_OK`；index_sha256 `ee4ca030…`；component_artifact_hash `e1db2f22…` | 本日志 |
| 环境/索引 | index + environment | `passed_gate_ids` 新增 `stage0.G7-LOGGING`；`next_task_id=stage0.09_checkpoint_and_resume` | `$DATA_ROOT/evidence/stage0/g7-formal/285023ce…/environment.json` |
| 本机回归 | `pytest tests/test_stage0_g7.py` | 6 passed | 本日志 |
| 三端 Git | `status`、`rev-parse`、`ls-remote`、SSH | `feat/stage0-completion`（本日志提交后再核对） | 本日志 |
| Agent 文档哈希 | `Get-FileHash SHA256`、`sha256sum` | 5/5 一致（本轮未修改） | 本日志 |

### 判定

- S0.8：完成，`stage0.G7-LOGGING` = PASS；正式索引、gate、replay 均通过；
  `full_g7_status=PENDING_S0.9`（完整 G7 可恢复性 gate 与 S0.9 共同形成）。
- 下一步：S0.9 checkpoint 与恢复，前置 G1/G4/G5/G6 与 S0.8 事件 schema/状态接口均 PASS。

### 问题、原因与风险

- 本次运行无失败；GPU 3 corrected volatile ECC 观察项与上一轮一致，未出现新增
  uncorrected ECC 或 Xid 事件。
- 追踪开销实测远低于阈值（组内 6.29%、最大成对 7.24%，阈值 10%），无 `APPROVED_EXCEPTION`。

### Git 与多端同步

- 本机/GitHub/服务器：`feat/stage0-completion`（本日志提交后再核对）。
- 服务器保留 `formal/run-f7566da` 锚点分支（G6/G7 证据锚点），`g7-suite`/`g7-formal`
  大容量证据仅存 `$DATA_ROOT`，不进入 Git。
- `Agent/*.md` 哈希：本机与服务器 5/5 一致（本轮未修改）。
- 临时文件：本轮运行日志与 PID 文件在 `$DATA_ROOT/tmp/`，同步确认后按精确路径清理；
  bundle 同样按精确路径清理。

### 下一步

- 提交本日志、推送 GitHub、bundle 快进服务器、切回 `feat/stage0-completion` 并核对三端。
- 随后执行 S0.9/G7-RECOVERY：`formalize_g7_recovery.py`（绑定 G7 index `285023ce…`）。

## 2026-08-05 16:55 CST — S0.9 启动复核与单卡 launcher 契约修复

### 目标与范围

- 复核 S0.8 正式证据与多端同步状态，确认 S0.9 启动前置（G1/G4/G5/G6 与 S0.8 事件
  schema/状态接口）全部通过后启动 S0.9/G7-RECOVERY。
- 本阶段处理：S0.9 代码审查、本机回归、正式套件首次执行；发现并修复单卡 local
  launcher 的 `rendezvous_id` 契约缺陷；随后以修复提交为新锚点重新执行正式套件。
- 不在本阶段处理：S0.10 及之后任务；系统级管理员动作。

### 实际修改

- 代码、配置、文档：
  - `src/param_importance_nlp/stage0_g7_recovery.py`：`_config_overrides` 中单卡
    （world_size=1）launcher 的 `rendezvous_id` 由固定字符串改为 `None`，满足
    `ResolvedConfigV2` 对 local launcher 的契约（local backend/world_size=1/local
    init/rendezvous_id=None）。
  - `tests/test_stage0_g9.py`：新增
    `test_g7_recovery_single_worker_local_launcher_is_contract_valid` 回归测试。
  - 追加本日志。
- 服务器或外部状态：在锚点分支 `formal/run-f7566da` 首次执行 S0.9 正式套件失败，
  未产生正式证据；失败日志保留在 `$DATA_ROOT/tmp/g7-recovery-formal-20260805-084704.log`。
- 用户原有修改：无；修复前本机与服务器工作树均干净。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| S0.8 正式索引 | 读取 G7 index | `status=PASS`；`gate_record.status=PASS`；`next_task_id=stage0.09_checkpoint_and_resume`；`checked_at 2026-08-05T08:28:27Z` | `$DATA_ROOT/evidence/stage0/g7-formal/285023ce…/index.json` |
| S0.8 开销报告 | 读取 overhead 对象 | `max_tracking_overhead_fraction=0.0629`（≤0.10）；paired_repetitions=3；sensitive_pattern_hits=0 | `$DATA_ROOT/evidence/stage0/tasks/08-285023ce…/objects/logging_overhead_report/…` |
| S0.8 environment | environment.json | `passed_gate_ids` 含 `stage0.G7-LOGGING`；`next_task_id=stage0.09_checkpoint_and_resume` | `$DATA_ROOT/evidence/stage0/g7-formal/285023ce…/environment.json` |
| 三端 Git | `status`、`rev-parse`、`ls-remote`、SSH | 本机/GitHub/服务器均 `feat/stage0-completion` @ `bd372af`，工作树干净 | 本日志 |
| Agent 文档哈希 | `Get-FileHash SHA256`、服务器 `sha256sum` | 5/5 文件哈希一致 | 本日志 |
| GPU 快照 | `nvidia-smi` | 4/4 卡空闲；无计算进程；uncorrected ECC=0 | 本日志 |
| S0.9 首次正式运行 | `formalize_g7_recovery.py --generator-git-commit f7566da --g7-logging-index-ref evidence/stage0/g7-formal/285023ce…/index.json` | 失败：`G7_RECOVERY_FORMAL_TASK_NOT_PASS:FAIL:ConfigContractError: local launcher 必须是 local backend/world_size=1/local init` | `$DATA_ROOT/tmp/g7-recovery-formal-20260805-084704.log` |
| 本机回归（修复后） | `pytest tests/test_stage0_g7_recovery.py tests/test_task_run_spec_resume.py tests/test_stage0_g9.py::test_g7_recovery_single_worker_local_launcher_is_contract_valid` | 7 passed | 本日志 |

### 问题、原因与风险

- 首次正式运行失败根因：`_config_overrides` 对 world_size=1 仍生成非空
  `rendezvous_id`，与 `ResolvedConfigV2` 的 local launcher 契约冲突；该分支此前未被
  G7-LOGGING（world_size=4）覆盖，S0.9 首次进入单卡 worker 配置时暴露。
- 修复：单卡 launcher 的 `rendezvous_id` 置为 `None`，并新增回归测试；修复后本机
  S0.9 相关测试全部通过。
- 风险：修复改变了 S0.9 证据的 generator 提交，正式证据将绑定到新提交，而不是
  `f7566da`；S0.7/S0.8 证据仍绑定 `f7566da`，不受影响。正式套件将在新锚点分支重跑。

### Git 与多端同步

- 本机/GitHub/服务器：修复提交前基线 `feat/stage0-completion` @ `bd372af`；本次修复
  提交后重新核对三端。
- 服务器锚点分支：`formal/run-f7566da` 仅保留为 G6/G7-LOGGING 证据锚点；S0.9 将使用
  新的 `formal/run-<short>` 锚点分支。
- `Agent/*.md` 哈希：本机与服务器 5/5 一致（本轮未修改）。

### 下一步

- 提交修复与本日志、推送 GitHub、用已验证 bundle 快进服务器并创建新锚点分支。
- 在新锚点分支以新 generator 提交重新执行 S0.9 正式套件并核验 G7 完整 gate。

## 2026-08-05 17:40 CST — S0.9 正式链第二次缺陷修复（正式 provider）

### 目标与范围

- 在 8b1d4d3 锚点完整重生成 bootstrap→G3→G4→G5→G6→G7→G7-RECOVERY 正式链，定位并
  修复 G7-RECOVERY 单卡 baseline 的 tiny provider 问题。
- 不在本阶段处理：S0.10 及之后任务；系统级管理员动作。

### 实际修改

- 代码、配置、文档：
  - `src/param_importance_nlp/stage0_g7_recovery.py`：新增
    `_FORMAL_RECOVERY_PROVIDERS`（`offline_hf/causal_lm/pile`），
    `build_stage0_g7_recovery_config` 不再继承 G7-LOGGING 的 tiny provider，
    改为显式使用正式离线 provider。
  - `tests/test_stage0_g9.py`：新增
    `test_g7_recovery_uses_formal_offline_hf_provider` 回归测试。
  - 追加本日志。
- 服务器或外部状态：
  - 8b1d4d3 锚点完整链：bootstrap/G3/G4/G5/G6/G7 均 PASS（G7-LOGGING 新证据）；
    G7-RECOVERY 失败，错误 `G7_RECOVERY_CHILD_FAILED:single-baseline:
    FORMAL_TRAINING_MUST_NOT_USE_TINY_PROVIDER`。
  - 按先例把当前 G3 控制面与三个 GLUE derived 资产移入
    `$DATA_ROOT/tmp/g3-pre-8b1d4d3-backup-20260805T090149Z/`，随后新链重建。
- 用户原有修改：无；本机与服务器工作树均干净（锚点分支运行）。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| 8b1d4d3 链 bootstrap | `bootstrap_formal_stage0.py` | `status=PASS` | `$DATA_ROOT/evidence/stage0/bootstrap/8b1d4d32…/index.json` |
| G3 attest/verify/materialize | `attest_g3_materialization.py`、`verify_g3_assets.py`、`materialize_and_publish_g3.py` | 13/13 assets；resolution `7acb7d5a…` | `$DATA_ROOT/manifests/evidence/g3/acquisition/b8c3a0fd…json`、`reports/stage0/g3/7acb7d5a…` |
| formal G3/G4 | `formalize_g3.py`、`formalize_g4.py` | 均 PASS | `$DATA_ROOT/evidence/stage0/g3-formal/7acb7d5a…`、`g4-formal/7acb7d5a…` |
| formal G5 | `formalize_g5.py` | PASS（14 子进程） | `$DATA_ROOT/evidence/stage0/g5-formal/c0547a19…/index.json` |
| formal G6 | `formalize_g6.py` | PASS（四卡） | `$DATA_ROOT/evidence/stage0/g6-formal/…` |
| formal G7-LOGGING | `formalize_g7.py` | PASS | `$DATA_ROOT/evidence/stage0/g7-formal/…` |
| G7-RECOVERY | `formalize_g7_recovery.py` | 失败：`FORMAL_TRAINING_MUST_NOT_USE_TINY_PROVIDER` | `$DATA_ROOT/tmp/g7-recovery-chain-8b1d4d3-part2.log` |
| 本机回归（修复后） | `pytest tests/test_stage0_g7_recovery.py tests/test_task_run_spec_resume.py tests/test_stage0_g9.py::test_g7_recovery_single_worker_local_launcher_is_contract_valid tests/test_stage0_g9.py::test_g7_recovery_uses_formal_offline_hf_provider` | 8 passed | 本日志 |

### 问题、原因与风险

- 根因：`build_stage0_g7_recovery_config` 从 G7-LOGGING 状态继承 `providers`
  （G7-LOGGING 为 CPU tiny fixture），而正式训练契约禁止 tiny provider；
  G5/G6 显式使用 `offline_hf/causal_lm/pile`，S0.9 未同步该覆盖。
- 修复：S0.9 主配置显式使用 `_FORMAL_RECOVERY_PROVIDERS`（offline_hf），单卡/四卡
  worker 均继承该 provider；新增回归测试。
- 风险：修复再次改变 generator 提交，需要在新锚点完整重生成正式链；8b1d4d3 链的
  中间证据保留为不可变历史。GLUE derived 与 G3 控制面在新链前再次移入 tmp 备份。

### Git 与多端同步

- 本机/GitHub/服务器：修复前基线 `feat/stage0-completion` @ `8b1d4d3`；
  本次修复提交后重新核对三端。
- 服务器锚点分支：`formal/run-8b1d4d3` 保留为 8b1d4d3 证据锚点；下一轮使用新
  `formal/run-<short>`。
- `Agent/*.md` 哈希：本机与服务器 5/5 一致（本轮未修改）。

### 下一步

- 提交修复与本日志、推送 GitHub、bundle 快进服务器、创建新锚点分支。
- 在新锚点完整重生成正式链并核验 G7 完整 gate。

## 2026-08-05 22:10 CST — S0.9 正式链第三次缺陷修复（禁用单 microbatch 重要性）

### 目标与范围

- 在 5d6daf8 锚点续跑 G7-LOGGING/G7-RECOVERY：G7-LOGGING 已 PASS；G7-RECOVERY
  单卡 baseline 暴露重要性估计器与单 microbatch 的契约冲突，定位并修复。
- 同时处理隧道故障与 Pile 下载暂停/恢复：按用户授权暂停下载以消除 G7 测量竞争，
  完成 S0.9 后恢复。
- 不在本阶段处理：S0.10 及之后任务；系统级管理员动作。

### 实际修改

- 代码、配置、文档：
  - `src/param_importance_nlp/stage0_g7_recovery_worker.py`：新增
    `_IMPORTANCE_ENABLED: Final = False`，TrainingRunSpec 不再启用重要性统计；
    与 G5 一致（单 microbatch 不满足 U 估计器，Stage 0 不验证重要性）。
  - `tests/test_stage0_g7_recovery.py`：新增
    `test_g7_recovery_worker_disables_importance_for_single_microbatch`。
  - 追加本日志。
- 服务器或外部状态：
  - 隧道：VPS 上陈旧/卡死隧道会话经授权清理两次（PID 401641/401694、
    535570/535615），`start.cmd` 自动重建新隧道；lab-pc 与 sophgo13 恢复可达。
  - Pile 下载：lab-pc 计划任务 `CjlPileFull`/`CjlPileFullSupervisor` 当前为
    “就绪”（未运行）；shard 10 `.part` 停在 9,213,636,608 bytes。按用户授权终止
    孤儿下载进程（235155/235196/235198），确保 G7 测量无竞争；S0.9 完成后恢复。
  - 失败 G7 残留套件 `g7-suite/3f95ebf9…` 已移入
    `$DATA_ROOT/tmp/g7-suite-5d6daf8-partial-20260805T135240Z/`。
- 用户原有修改：无；本机与服务器工作树均干净（锚点分支运行）。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| G7-LOGGING（5d6daf8，无下载竞争） | `formalize_g7.py` | `status=PASS`；`G7_REF=evidence/stage0/g7-formal/75ceebdb…/index.json` | `$DATA_ROOT/evidence/stage0/g7-formal/75ceebdb…/index.json` |
| G7-RECOVERY（5d6daf8） | `formalize_g7_recovery.py` | 失败：`G7_RECOVERY_CHILD_FAILED:single-baseline: IMPORTANCE_ESTIMATOR_REQUIRES_AT_LEAST_TWO_MICROBATCHES` | `$DATA_ROOT/tmp/g7-recovery-part2-5d6daf8.log` |
| 本机回归（修复后） | `pytest tests/test_stage0_g7_recovery.py tests/test_task_run_spec_resume.py tests/test_stage0_g9.py::test_g7_recovery_single_worker_local_launcher_is_contract_valid tests/test_stage0_g9.py::test_g7_recovery_uses_formal_offline_hf_provider` | 9 passed | 本日志 |
| 隧道诊断 | VPS `ss -ltnp`、`ssh -G lab-pc/sophgo13-via-lab` | 陈旧隧道占 50023/22022；清理后新隧道（如 PID 537938）重建，两端可达 | 本日志 |
| 下载暂停 | `kill 235155 235196 235198` | `NO_CURL`；shard 10 `.part` 9,213,636,608 bytes | 本日志 |

### 问题、原因与风险

- G7-RECOVERY 失败根因：worker 的 TrainingRunSpec 启用重要性统计，而 S0.9
  单卡配置每 step 只有 1 个 microbatch，`u` 估计器要求至少 2 个；G5 已禁用
  重要性，S0.9 未同步。
- 修复：`_IMPORTANCE_ENABLED=False`，回归测试覆盖。
- 隧道反复卡死的现象：VPS 上旧隧道会话占用端口且数据通道失效；清理后
  `start.cmd` 重建。若再卡死按同一精确 PID 流程处理。
- 风险：修复再次改变 generator 提交，需要新锚点完整重生成正式链；Pile 下载暂停
  期间不产生竞争，S0.9 完成后再恢复（计划任务 `CjlPileFullSupervisor`）。

### Git 与多端同步

- 本机/GitHub/服务器：修复前基线 `feat/stage0-completion` @ `5d6daf8`；
  本次修复提交后重新核对三端。
- 服务器锚点分支：`formal/run-5d6daf8` 保留为 5d6daf8 证据锚点；下一轮使用新
  `formal/run-<short>`。
- `Agent/*.md` 哈希：本机与服务器 5/5 一致（本轮未修改）。

### 下一步

- 提交修复与本日志、推送 GitHub、bundle 快进服务器、创建新锚点分支。
- 在新锚点完整重生成正式链（bootstrap→G7-RECOVERY），核验 G7 完整 gate。
- S0.9 通过后恢复 Pile 下载（启动 `CjlPileFullSupervisor`），并核对 shard 10 续传。

## 2026-08-05 23:30 CST — S0.9 正式链第四次缺陷修复（interrupt 阶段 observer 注册时序）

### 目标与范围

- 在 a82caca 锚点完整重生成正式链：bootstrap/G3/G4/G5/G6/G7-LOGGING 均 PASS；
  G7-RECOVERY 单卡 interrupt 阶段失败，定位并修复 observer 注册时序。
- 不在本阶段处理：S0.10 及之后任务；系统级管理员动作。

### 实际修改

- 代码、配置、文档：
  - `src/param_importance_nlp/stage0_g7_recovery_worker.py`：interrupt 阶段改为在
    首次 `engine.run()` 之前注册 `tail_trace` observer（满足
    `TRAINING_OBSERVER_MUST_REGISTER_BEFORE_RESUME_OR_RUN`），首次运行后清空其
    记录，再用于孤儿尾段运行。
  - 追加本日志。
- 服务器或外部状态：a82caca 链的 G7-RECOVERY 失败产物保留为不可变历史；
  Pile 下载继续保持暂停，避免 G7 测量竞争。
- 用户原有修改：无；本机与服务器工作树均干净（锚点分支运行）。

### 实验与验证

| 项目 | 命令/配置 | 结果 | 证据路径 |
|---|---|---|---|
| a82caca 链 bootstrap/G3/G4/G5/G6/G7-LOGGING | `g7-recovery-chain-a82caca.sh` | 全部 PASS；G7_REF=`evidence/stage0/g7-formal/28e0807b…/index.json` | `$DATA_ROOT/evidence/stage0/…` |
| G7-RECOVERY（a82caca） | `formalize_g7_recovery.py` | 失败：`G7_RECOVERY_EXPECTED_INTERRUPT_NOT_OBSERVED:single`；worker stderr：`TRAINING_OBSERVER_MUST_REGISTER_BEFORE_RESUME_OR_RUN` | `$DATA_ROOT/tmp/g7-recovery-chain-a82caca.log`、`g7-recovery-suite/e6e4a49c…/transcripts/single-interrupt.json` |
| 本机回归 | `pytest tests/test_stage0_g7_recovery.py tests/test_task_run_spec_resume.py tests/test_stage0_g9.py::…` | 9 passed | 本日志 |

### 问题、原因与风险

- 根因：interrupt 阶段先运行引擎到边界（step 2），再调用
  `engine.register_observer(tail_trace)`；`TrainingEngine` 禁止在已有 attempt
  记录后注册 observer，因此 worker 在写入中断 marker 前抛出异常。
- 修复：`tail_trace` 在首次 run 前注册，首次 run 后清空 `steps`，只记录孤儿尾段
  （step 3）样本轨迹。
- 风险：修复再次改变 generator 提交，需要新锚点完整重生成正式链；下载保持暂停。

### Git 与多端同步

- 本机/GitHub/服务器：修复前基线 `feat/stage0-completion` @ `a82caca`；
  本次修复提交后重新核对三端。
- 服务器锚点分支：`formal/run-a82caca` 保留为 a82caca 证据锚点；下一轮使用新
  `formal/run-<short>`。
- `Agent/*.md` 哈希：本机与服务器 5/5 一致（本轮未修改）。

### 下一步

- 提交修复与本日志、推送 GitHub、bundle 快进服务器、创建新锚点分支。
- 在新锚点完整重生成正式链并核验 G7 完整 gate。
- S0.9 通过后恢复 Pile 下载（启动 `CjlPileFullSupervisor`），并核对 shard 10 续传。

## 2026-08-06 01:20 CST — S0.9 阻塞：四卡 NCCL 死锁（G6 无法通过）

### 目标与范围

- 记录 9da2af6 锚点 G6 三次失败的根因定位与当前阻塞，等待管理员处理 GPU/NCCL。
- 不在本阶段处理：S0.10 及之后任务；不做 GPU 重置/驱动重载/重启等管理员动作。

### 实际状态

- 9da2af6 链：bootstrap/G3/G4/G5/G7-LOGGING 均 PASS（G5 `eef99053…`，
  G7-LOGGING `28e0807b…` 为 a82caca 链；9da2af6 链中 G7-LOGGING 未到）；G6 三次失败。
  更正：9da2af6 链中 G5 `eef99053…` 通过；G6 三次失败均为 `collective-00`。
- G6 失败证据：
  - 第 1 次：外层超时（421s，SIGTERM）。
  - 第 2 次：4 rank SIGABRT（NCCL watchdog）。
  - 第 3 次（带 NCCL_DEBUG=INFO）：`WorkNCCL(OpType=ALLGATHER, NumelIn=394,
    NumelOut=1576, Timeout(ms)=300000) ran for 300035ms before timing out`。
- 官方 `run_cuda_nccl_smoke.py` 四卡 all-reduce 两次均挂死（默认环境与
  `NCCL_P2P_LEVEL=PCI + NCCL_SHM_DISABLE=1` 调优环境，240s 限时均超时）。
- GPU 表面健康：4/4 空闲、温度 37–49°C、uncorrected ECC=0、无 Xid、无残留进程。
- 服务器另一 Codex 代理正在根盘下载 `Qwen/Qwen3.6-27B`（`/home/sophgo13/lcl/…`，
  约 13GB/54GB，已运行约 2.5h）；其与 DATA_ROOT 不同盘，a82caca 的 G6 在其运行中
  曾通过，初步判断非直接原因，但作为竞争负载一并报告。

### 问题、原因与风险

- 现象：四 rank NCCL 在极小 ALLGATHER 上死锁 300s，随后 watchdog 中止；基础
  NCCL smoke 同样挂死。属于 GPU/NCCL 通信健康问题，不是 S0.9 代码缺陷。
- 历史背景：本机此前为 G6 稳定通过曾引入 `NCCL_P2P_LEVEL=PCI`、关闭 SHM/P2P
  的调优（5513c41/eed7064），当前代码未含这些变量；本次调优环境重试仍挂死。
- 风险：若硬件/驱动状态持续异常，必须由管理员按 G0-G 流程处理（GPU 重置/驱动
  重载/重启），Agent 不得自行重置设备或修改系统环境。

### 下一步

- 等待用户/管理员处理 GPU/NCCL 健康问题（重置或重启）。
- 恢复后无需改代码/锚点：直接以 9da2af6 + G5_REF `eef99053…` 从 G6 续跑
  G6→G7→G7-RECOVERY。
- S0.9 通过后恢复 Pile 下载（启动 `CjlPileFullSupervisor`）并核对 shard 10 续传。
