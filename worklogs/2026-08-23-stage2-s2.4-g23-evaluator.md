# 2026-08-23 — G2.3 independent reference evaluator

## 范围与身份

本任务在 `feat/stage2-s204-g23-evaluator` 隔离 worktree 实现，未修改主工作树 presentation，也未修改 `ops/stage2/run_s204_formal.py`。evaluator 只接受六个 formal Stage2.04 `TaskRunResult` 路径；所有指标由 TaskArtifactStore commit、content-addressed tensor bundle 与 sizing/final resume commits 重算。

## Fail-closed 口径

缺少 checkpoint hash、共同 producer commit、相邻 sizing raw vectors、全部候选 `delta_sci(B)`、高精度/累加对照或 model/RNG/replay hash 时，cell 为 `BLOCKED`，六 cell 未全部 COMPLETE 时不会形成 formal PASS。候选 artifact 中调用者填写的布尔/数字被忽略。结果保存到 `g2.3-attempts/<artifact_hash>/evaluation.json` 并以 append-only hash index 记录；新聚合输入产生新 attempt，不会被旧的部分 attempt 永久锁定。

## 验证

| 项目 | 命令 | 结果 |
|---|---|---|
| evaluator 单测 | `python -m pytest --basetemp .agent-temp/pytest/s204-g23-evaluator-unit3/base -o cache_dir=.agent-temp/pytest/s204-g23-evaluator-unit3/cache tests/test_stage2_g23_evaluator.py -q` | 5 passed |
| Python 编译 | `python -m py_compile src/param_importance_nlp/experiments/stage2_g23_evaluator.py ops/stage2/evaluate_s204_g23.py` | exit 0 |
| 空输入 CLI | `python ops/stage2/evaluate_s204_g23.py --workspace-root . --output-root <controlled-output>` | `NOT_RUN`, exit 3 |

正式 G2.3 未运行；本地测试只验证合同、tamper、缺 cell、非有限值、阈值边界和版本化/内容寻址重判。
