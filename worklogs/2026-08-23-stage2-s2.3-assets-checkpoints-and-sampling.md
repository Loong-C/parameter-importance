# 2026-08-23 — S2.3 模型、checkpoint、数据与采样框

## 目标与范围

- 完成 `stage2.03_assets_checkpoints_and_sampling` 的资产/训练阶段/经验分布/独立抽样 manifest 合同与本机可重放开发验证。
- 服务器只读盘点并复用已验收 14M/31M step0、Pile shard-0 与 index；不读取或改变活动 `.part`、锁和临时元数据。
- 不把缺失的 early≈1% 或 mid_late≈50% checkpoint 伪装成正式输入；因此本轮 `G2.2` 明确为 BLOCKED，`G2.2-dev` 仅表示 local fixture/step0 开发边界。

## 服务器资产盘点与安全修复

| 资产 | 路径/身份 | 结果 |
|---|---|---|
| Pythia 14M initialization | `$DATA_ROOT/models/pythia-14m-step0/model.safetensors`; 28,143,920 bytes; SHA-256 `495e509ae82ddbe9cf39558a13b10973822c7bcedd0c6fd7d3225620b9e2edb5`; revision `56079904bb80b7f36d3b794089f146e7a4d6efae` | READY；仅有全局 manifest，目录内 manifest/SHA256SUMS 缺失，已显式记录 |
| Pythia 31M deduped initialization | `$DATA_ROOT/models/pythia-31m-deduped-step0/model.safetensors`; 121,987,544 bytes; SHA-256 `5dad0c31b7c19af86f5840674fa4f16a91bdf617245aae17387bdaedd3600319`; revision `73628c85dd9d12d43c07be77ebcf10cef5fd9660` | READY；两份 BOM manifest 已按固定 repair 流程规范化 |
| Pile empirical range | `$DATA_ROOT/datasets/pile-deduped-pythia-preshuffled/document-00000-of-00020.bin` 30,000,000,000 bytes, SHA-256 `1ce355bd2683627d0ff689f8578115cf3df84bd1edf3410e6aca9705d31fc6ea`; `document.idx` 1,757,184,042 bytes, SHA-256 `1d9fdd760295eb2007a4874440b27c559ca722239fa2814aa8a2ee6724b7852f` | allowlist 固定为两文件，sample ID `[0,524288)`；排除 shard 1–4、shard 5 `.part`、锁和 meta |
| Missing checkpoints | `$DATA_ROOT/models` | 14M/31M 的 early 与 mid_late 均不存在；服务器 HF 端点探针超时，未重复下载 |

31M 两份原始 BOM manifest（模型目录与全局 manifest）原始 SHA-256 均为 `1f7f812bb522ddb344d5ae10e66b94dc4e11ac9049e7eedbb737350719a24e59`。在唯一 `$DATA_ROOT/tmp/stage2/s23-manifest-repair-20260823-01` 临时目录生成 UTF-8 无 BOM JSON，标准解析后仅原子替换两个精确目标；规范 SHA-256 均为 `a9431853c8bba9249fc897661651da0108382154de41abe9843c52c698b860ea`，repair evidence 为 `$DATA_ROOT/manifests/stage2/s23-manifest-repair-20260823-01.json`（SHA-256 `f3f7deec2019dbc96a8742a2785f0aeab751a24f63de04e90443d45cbd1833a0`）。权重、Pile 对象、活动 `.part`/lock 均未触碰。

## 实际修改

- 新增 `stage2_assets.py`：严格 checkpoint matrix（两模型×三阶段）、固定 Pile 两文件 allowlist/data-range、manifest repair、资产状态与哈希绑定；ready 必须具备 revision、文件哈希和 offline-load evidence，缺失项只能为 blocked。
- 扩展 `sampling.py`：五条 stream 的 generator 起止状态摘要、可重放 `stage2-draw-stream-manifest-v1`、nested repetition mapping v1；保留 sample-ID collision，不以交集为独立性定义。
- S2.3 fixture runner 输出五条 stream manifest、mapping hash 与六格 fixture asset matrix；formal provider 仍拒绝缺失/未验收资产，不从 synthetic fallback 晋级。
- 新增五个 Stage 2 schema、资产/抽样 source config，并为 CLI 增加严格 artifact dispatch。
- 根工作树 `presentation/` 用户修改未触碰、未暂存。

## Gate 与验证

| Gate/项目 | 结果 | 证据 |
|---|---|---|
| G2.2-dev | PASS_LOCAL_FIXTURE_ONLY；14M step0/fixture draw stream 可重放，不能解锁正式 reference/matrix | `tests/test_stage2_assets_checkpoints_sampling.py` |
| G2.2 | BLOCKED_MISSING_FOUR_CHECKPOINTS；唯一最小阻断为两模型 early + mid_late 固定 revision/文件/offline-load evidence 缺失 | 本文件资产盘点与 source config |
| S2.3/现有采样与 runner 定向回归 | 30 passed，exit 0 | `.agent-temp/pytest/s203-target-01/` |
| 新资产/checkpoint/schema/抽样测试 | 6 passed，exit 0 | `.agent-temp/pytest/s203-target-03/` |
| CLI 修复后的新增 dispatch 用例 | 1 passed，exit 0 | `.agent-temp/pytest/s203-target-05/` |
| Stage 2/3 specialized chain | 1 passed，exit 0 | `.agent-temp/pytest/s203-target-06/` |

第一次 44 用例组合回归发现新 draw schema 被通用 `stage2-*` validator 先行拦截；根因为 dispatch 顺序，已将五个新 schema 分支置于通用 Stage 2/3 分派之前，并只重跑失败用例通过。失败不涉及科学产物或服务器资产。

## 证据身份与同步

- producer/execution commit：`2eb42f2`（S2.3 code/schema/config/tests producer）；此前消费的 S2.1 preregistration producer 为 `c774b27e5005d59840e3c5844fbf690ccf47d733`，其 hash 未改写。
- consumer commit：主代理合并/消费本分支时记录；sampling/asset schema 变化只影响 S2.3 及其下游，不使 S2.1 preregistration 失效。
- 服务器修复证据独立于 Git，权威路径、大小、revision、SHA-256 与 repair evidence 已记录；未声明服务器执行对齐到本分支。

## 下一步

- 主代理审阅并推送本分支；后续仅在固定 upstream revision 可达、最小文件集下载并通过哈希/离线加载后解除 G2.2 blocker。
- S2.4/S2.6 继续只消费已冻结 sizing/pilot schema；最终 reference A/B 与 confirmatory draw manifest 在对应规模冻结后一次生成。

## 2026-08-23 14:01 CST — 最终资产发布、DAG 修复与交接收尾

### 目标与范围

- 本节是 S2.3 的 append-only 收尾，补记最终 formal 资产/数据冻结、代码与 DAG 修复、发布恢复及交接验收；不修改 S2.1 预注册口径，不跳过 S2.3，也不把外部正式证据复制进 Git。
- 早先记录的 `G2.2=BLOCKED_MISSING_FOUR_CHECKPOINTS` 属于发布前快照；本节记录的最终六 checkpoint 与数据 manifest 已完成冻结并通过正式身份校验。

### 代码、DAG 与发布恢复

- 修复 S2.3 predecessor/DAG 消费关系及相关 loader/identity 校验；主代理最终组合回归 `28 passed`，exit 0。最终代码/执行提交为 `676ab436422b1a514bddfc1181d0645fed4de7be`。
- 两次发布尝试分别失败后，未跳过任务、未覆盖旧证据；改用逐文件修复与 base64 固定命令完成发布/修复，失败记录保留在 `tmp/stage2/s203-hf-fixed-20260823-01/publish.log` 与 `tmp/stage2/s203-hf-fixed-20260823-02/repair.log`。`stage2_assets`/数据生成器幂等重跑 PASS。
- 六个 checkpoint 目录各自 `SHA256SUMS` 均为 `7/7 PASS`；六个目录的 tracked loader 与 `validate_formal_asset_identity` 均 PASS。6 个 CPU offline forward/backward smoke 均 PASS；未使用 synthetic fallback。

### 冻结数据与六 checkpoint

- 六个 checkpoint 均为 `state=ready` 且离线加载证据 PASS：14M 的 initialization/early/mid_late（step 0/1000/71000）与 31M-deduped 的 initialization/early/mid_late（step 0/1000/71000）。选择 manifest：`manifests/stage2/s203-selection.json`。
- 数据范围冻结为 `dataset_id=dbbfeb12bab4027b386bd97d604d8134699e96f79e309cceacff7999a55b5dad`、revision `4647773ea142ab1ff5694602fa104bbf49088408`、sample ID `[0,524288)`、sequence length `2048`、两文件 allowlist（`document-00000-of-00020.bin` 与 `document.idx`）；`.part`、lock、meta 与 shard 1–4 均排除。
- 最终 content digest：asset `f57decd5cf00e69e45ab2f02c994abb202f5c614e1441acb8aebcb1807ff76ee`；data `df8eeac5178305d409cf6128ac5d5648567aae895592c79fa21542e84a28e0f1`。

### 最终文件身份

| 文件 | 权威路径（服务器） | SHA-256 |
|---|---|---|
| data range | `manifests/stage2/s203-data-range.json` | `a6a633d30e55351679368ded522d4b697fc676e8f4ca7f63108406524459fb14` |
| checkpoint selection | `manifests/stage2/s203-selection.json` | `1035e831f63862c0d15549fe24738ffd1432e8e165628ba64b40248461dea9bd` |
| asset resolution | `manifests/stage2/s203-asset-resolution.json` | `1b1609ea97974a560b4da98707eecdbbbd97010e067e9899234079ab7b6cfb20` |
| G2.2 evidence | `evidence/stage2/s203/g2.2-assets.json` | `b6805b6744374e7d05f193db4d72162176b930fa1db250bc00797b3ad30528a8` |

上表只登记外部正式证据的受控路径与哈希，不把文件复制进 Git。31M BOM 修复仅去除 UTF-8 BOM，原子替换目标文件；权重、Pile 对象和活动 `.part`/lock 未被触碰。

### 证据身份与同步

- 最终资产 resolution 的 `producer_commit`/`execution_commit` 均为 `676ab436422b1a514bddfc1181d0645fed4de7be`；此前 `2eb42f2` 仅保留为早期 S2.3 code/schema/tests producer，不改写其历史身份。
- 最终 content digest、文件 SHA、checkpoint revision 与 data-range identity 均由服务器正式 manifest 绑定；本机 worklog 是 consumer 文档，不改变外部产物身份。
- server repo clean，HEAD=`676ab436422b1a514bddfc1181d0645fed4de7be`。本次不声明将外部正式证据复制进 Git；服务器执行端已处于与最终 execution commit 对齐的干净状态。

### 下一步

- S2.3 交付完成；S2.4/S2.5 可按 DAG 消费上述冻结 identity。后续如需改变 checkpoint、数据范围或抽样身份，必须追加 amendment/新 manifest，不覆盖本节记录。
