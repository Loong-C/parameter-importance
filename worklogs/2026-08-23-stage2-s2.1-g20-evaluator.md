# 2026-08-23 — S2.1 G2.0 独立 evaluator

## 目标与范围

- 新增独立 G2.0 formal evaluator；输入限定为 `stage2.01_scope_hypotheses_and_preregistration` 的三个 formal TaskArtifact commit（`preregistration`、`hypothesis_contract`、`gate_record`）。
- 重读并重建预注册/H1–H6 合同，绑定冻结 plan、`docs/mathematics.md`、Stage 1 report、输入 config hash、evaluator producer commit/source hash 与 evaluation config hash。
- runner 的 gate candidate 只接受 `NOT_RUN`、`formal_eligible=false` 作为“未自签”证明；不接受其数字或 PASS 状态。

## 实际修改

- `src/param_importance_nlp/experiments/stage2_g20_evaluator.py`：fail-closed G2.0 evaluator、GateRecord 构造与 formal envelope 发布。
- `src/param_importance_nlp/experiments/preregistration.py`：新增严格 H1–H6 contract validator（重建内容比较，禁止只改 hash）。
- `ops/stage2/evaluate_s201_g20.py`：CLI（仅 commit refs，异常返回 blocked/exit 3）。
- `tests/test_stage2_g20_evaluator.py`：三 commit formal TaskArtifact PASS、重复/错误/混合 config/local fixture、self-signed candidate、payload/document tamper。

## 验证

| 项目 | 命令 | 结果 |
|---|---|---|
| G2.0 evaluator 定向测试 | `python -m pytest --basetemp .agent-temp/pytest/s201-g20-02/basetemp -o cache_dir=.agent-temp/pytest/s201-g20-02/cache tests/test_stage2_g20_evaluator.py -q` | 6 passed |
| prereg + evaluator 回归 | 同上 `tests/test_stage2_preregistration.py tests/test_stage2_g20_evaluator.py` | 12 passed |
| Stage 2 runner/artifact 回归 | `tests/test_stage23_task_runners.py tests/test_stage2_formal_handoff_adapter.py tests/test_task_artifacts_and_default_runners.py` | 33 passed |
| CLI fail-closed smoke | `python ops/stage2/evaluate_s201_g20.py --workspace-root . --output-root . --artifact-ref nope --artifact-ref nope --artifact-ref nope` | exit 3，GateRecord BLOCKED |

## 证据身份与有效性

- evaluator output envelope 使用输入三 commit 的共同 `config_hash`，source_refs 按 canonical artifact-kind 顺序绑定三 commit；PASS 仅在 envelope 成功重新发现后暴露 `formal_eligible=true`。
- 计划 hash 固定为 `9af31ee0b82cbb0526817c7bb741ca708e33f96e7be3dda65d54b188e942fe12`；数学文档/Stage 1 report hash 从 prereg provenance 读取并核对当前文件。
- 本机测试临时目录均位于 `.agent-temp/pytest/`，未产生仓库外临时证据。

## 下一步

- 提交并推送 `feat/stage2-s201-g20-evaluator`，交由 materializer 使用 `evaluate_formal_g20` 或 CLI。
