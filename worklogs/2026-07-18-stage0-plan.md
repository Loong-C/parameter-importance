# 2026-07-18 第 0 阶段计划编写

- 任务范围：通读 `Agent/` 五份文档和 `plan/general_plan.md`，结合本机、GitHub、lab-pc 链路和 sophgo13 服务器现状，编写第 0 阶段环境配置与实验基础设施建设计划。
- 当前状态：计划编写与本地核验完成；因同一工作树正并行生成 Stage 1–3 文档，未擅自暂存、提交、推送或同步这些混合改动。
- 工作分支：`main`

## 2026-07-18 21:00 CST — 完成现状审计与计划拆分

### 目标与范围

- 本阶段要完成什么：将 Stage 0 拆为目的明确、步骤细粒度、验收标准可量化且带后续 gate 的子任务文档。
- 不在本阶段处理什么：不安装依赖、不修复 GPU、不改变 SSH/网络/系统配置、不启动训练、不修改活动 Pile 下载。

### 实际修改

- 新增 `plan/stage0/README.md`，记录当前基线、技术决策、12 个子任务、G0–G10 和 Stage 1–9 前置关系。
- 新增 `plan/stage0/01_*.md` 至 `12_*.md`，分别覆盖基线与安全、存储、环境、资产、配置与 provenance、单卡、四卡/梯度语义、日志、checkpoint、容量运维、自动测试重放和最终同步。
- 更新 `plan/general_plan.md`，增加详细 Stage 0 计划入口。
- 用户原有修改：保留并审查 `docs/mathematics.md` 的公式修复；仅移除造成 `git diff --check` 警告的多余文件末尾空行，未回滚公式内容。
- 服务器或外部状态：只读核验本机、GitHub 和服务器；没有写入服务器、lab-pc 或 GitHub。

### 现状结论

- 本机为 Windows 10、约 23.7 GiB 内存、无 NVIDIA GPU，适合作为编辑、轻量 CPU 检查和调度端，不适合作为 CUDA/NCCL gate。
- 服务器为 Ubuntu 24.04.3、约 1 TiB 内存；项目大盘约 2.9 TiB 可用，系统根盘只余约 15 GiB。
- 服务器底层驱动目录可见 8 张 A100，NVML/PyTorch 只枚举 7 张；一张可见卡记录 24 次不可纠正 ECC。旧 8 卡 `READY` 已失效，GPU 相关 gate 当前阻塞。
- 服务器现有 Python 3.12.3 venv 的 89 个锁定包与现场 freeze 一致，`pip check` 通过；系统 toolkit 12.9 与 PyTorch CUDA runtime 12.6 已在计划中分开处理。
- 现有模型包括 Pythia 14M、31M、160M；31M 顶层 manifest 因 UTF-8 BOM 不能由严格 JSON 解析器直接解析。Pythia 410M、MNLI、RTE 尚缺。
- Pile shard 5 仍由既有进程写入 `.part`；计划明确禁止竞争、改名或将其作为输入。
- 本机、GitHub 和服务器 `main` 当前均为 `34966d0819a5229569169bfe436afe9058c0ed24`；服务器干净。
- `Agent/` 五文件中只有 `worklogs.md` 两端不一致，最终 Stage 0 同步 gate 当前不通过。

### 实验与验证

| 项目 | 检查范围 | 结果 | 证据路径 |
|---|---|---|---|
| Agent 文档通读 | `Agent/*.md` 五文件 | 完成 | `Agent/` |
| 总计划通读 | Stage 0–9 与推进原则 | 完成 | `plan/general_plan.md` |
| 本机基线 | OS、CPU、内存、GPU、磁盘、Python/Git/SSH | 完成；无 CUDA GPU | 本日志、`plan/stage0/README.md` |
| 服务器基线 | OS、CPU、内存、磁盘、环境、GPU、资产 | 完成；发现 GPU/ECC 硬阻塞 | 本日志、`plan/stage0/README.md` |
| Git 三端基线 | 本机、GitHub、服务器 `main` | 三端 HEAD 一致 | 本日志 |
| Agent 哈希 | 五文件两端 SHA-256 | 四份一致，`worklogs.md` 不一致 | 本日志、`plan/stage0/01_baseline_and_safety.md` |
| 文档结构 | 12 个子任务的目的、步骤、产出、核验、gate | 全部字段存在 | `plan/stage0/` |
| 链接与空白 | 内部链接、行尾空白、文件末尾 | 通过 | 本地检查输出 |

### 产物与证据

| 路径 | 类型 | 大小 | SHA-256/revision | 验收状态 |
|---|---|---:|---|---|
| `plan/stage0/README.md` | 总览与 gate | 小型 Markdown | 随 Git 提交 | 通过本地结构检查 |
| `plan/stage0/01_*.md`–`12_*.md` | 子任务计划 | 小型 Markdown | 随 Git 提交 | 通过本地结构检查 |
| `plan/general_plan.md` | 总计划入口 | 小型 Markdown | 随 Git 提交 | 通过链接检查 |

### 问题、原因与风险

- GPU 8/7 枚举不一致和不可纠正 ECC 需要服务器管理员处理；本任务未尝试 GPU reset、驱动重载或系统修改。
- 旧环境 `READY` 对应不同内核与 GPU 状态，只能作为历史证据。
- 服务器根盘余量较少，计划要求所有环境、缓存、编译和运行产物落到项目大盘。
- 31M manifest 的 BOM 暴露 manifest 原子生成和严格 schema gate 的必要性。
- 活动 Pile 下载与 Stage 0 实施可能并行，任何资产任务都必须先核对锁与进程。

### Git 与多端同步

- 本机分支/HEAD：`main` / `34966d0819a5229569169bfe436afe9058c0ed24`（提交前）。
- GitHub 分支/HEAD：`main` / `34966d0819a5229569169bfe436afe9058c0ed24`（提交前）。
- 服务器分支/HEAD：`main` / `34966d0819a5229569169bfe436afe9058c0ed24`（提交前），工作树干净。
- `Agent/*.md` 哈希核对：四份一致；`worklogs.md` 本机与服务器不一致，待按 `Agent/sync.md` 审查同步。
- 工作树状态和临时 bundle 清理：尚未创建本次 bundle；待计划 QA、提交、推送和服务器快进后更新。

### 当时记录的后续动作

- 多视角文档 QA、完整差异、敏感信息和大文件检查已在下一节完成。
- 暂存、提交、推送、服务器快进和 `Agent/worklogs.md` 同步需等待并行 Stage 1–3 工作完成并统一审查，不能在混合工作树中抢先处理。

## 2026-07-18 21:24 CST — 完成多视角 QA 与本地交付检查

### QA 修订

- 将基线 gate 拆为 G0-C（访问、仓库、授权路径和 `Agent/` 同步）与 G0-G（GPU 枚举和健康），消除“GPU 阻塞但非 GPU 工作可继续”的歧义。
- 补齐 Git 大文件守卫、可复用资产获取入口、资产状态转换职责、完全离线重建和缺包负向演练。
- 明确定义 experiment、run、attempt、session、脏工作树完整补丁和 canonical/raw 日志 lineage。
- 补齐 DDP 默认 rank 平均下的全局 token 分母、world-size 缩放、不等 rank token 数、不等 microbatch、尾窗与 `no_sync` 通信核验。
- 补齐 DataLoader 已提交游标、worker RNG、预取丢弃和 `num_workers>0` 精确恢复。
- 建立统一数值/性能测量协议，固定 warmup、测量窗口、重复次数、聚合方式和性能例外责任。
- 将容量 gate 拆为 G8-C、G8-S4 和 G8-S5，明确 synthetic accumulator 只验证参数 shape/dtype 的容量上界，不实现重要性算法。
- 修正磁盘安全余量为 `E + max(0.2E, 100 GiB)`，并增加 G1-D 单盘故障域、获授权备份或明确风险接受的下游 gate。
- 增加 `general_plan.md` 要求到子任务、产出和 gate 的追踪表。
- 终审后进一步限定 canonical `optimizer_step` 事件基数，隔离分叉 TensorBoard，并定义 checkpoint object/commit/event/lineage 的提交顺序和崩溃 reconciliation。
- 将 G1 正式定义为 G1-B 与 G1-D 的合取；将性能/容量例外定义为 `APPROVED_EXCEPTION`、gate 定义为 `CONDITIONALLY_ACCEPTED`，并要求单独的 readiness 标记。
- 为 G8-S4/S5 增加参数状态容量包络和计算/通信工作包络，最终算法超出任一包络即自动重验。
- 统一多 worker 精确恢复的二选一路径：正式配置使用 prefetch 时必须通过等价测试，否则锁定 `num_workers=0` 并明确标为不支持多 worker。

### 本地核验

| 检查 | 结果 |
|---|---|
| Stage 0 Markdown 数量 | 13（总览 1 + 子任务 12） |
| 子任务必需章节 | 12/12 均包含目的、前置、实施步骤、产出、核验和 Gate |
| Markdown 内部链接 | 全部可解析 |
| 行尾空白与 `git diff --check` | 通过 |
| 敏感模式与嵌入 URL 扫描 | 0 命中 |
| 10 MiB 以上新增计划文件 | 0 |
| Stage 0 Markdown UTF-8 BOM | 0 |

### Git 与同步状态

- 核验期间同一工作树新增了不属于本任务的 `plan/stage1/`、`plan/stage2/`、`plan/stage3/` 和 Stage 3 工作日志，`plan/general_plan.md` 也出现并行的 Stage 1 链接修改。
- 为避免把并行任务的未完成文件混入本阶段提交，本任务没有执行暂存、提交、推送、bundle 或服务器写入。
- Stage 0 文档本地交付不受影响；GitHub/服务器 HEAD 和 `Agent/` 哈希仍以本日志上一节的提交前快照为准，不能宣称 G10 已通过。
