# S1.6：训练步集成、累计量与比较基线

## 任务边界

- 任务：`stage1.06_training_integration_and_accumulators`。
- 上游：严格 Schema 刷新后的 S1.5 `G1-EST` 正式 index
  `evidence/stage1/s1-5-formal/36a792b6a89045ae49c32225038a6c10d5082d2c/formal-20260815-s15-schema-r2/index.json`；
  文件 SHA-256 为 `890970c5886821377ca0409ca910184da99063432d3ef35fd3e3713a5e5514c5`，
  Gate artifact hash 为 `4bee73ef5053d78f77d688f94fd1737cc744cbcf2bb4774f8916fc70e466887a`。
- 目标：把 S1.5 纯估计器接入确定性单进程 CPU optimizer step，冻结 18 个 step 边界，证明
  raw/U 暂存与事务提交、signed/positive/negative-mass/absolute 累计、SGD/AdamW 数据位移与
  weight-decay 分解、GradScaler finite/skip 状态机以及统计开启/关闭无扰动，并发布
  `G1-STEP`。真实 Pythia、GPU、DDP 与跨进程同步仍属于 S1.7 以后任务。

## 实现与主代理验收

- 本任务由独立子代理使用 `gpt-5.6-terra`、`xhigh` 完成；主代理负责计划逐项反查、正式
  handoff、失败诊断、独立重放和发布后验收。
- 主实现提交为 `d214c42f5fc02d5843b3b73afb1fb7dde90371ab`：新增冻结训练 fixture、独立
  FP64 oracle、S1.6 evidence/replay/formalizer、8 份严格角色 Schema 和专项测试；扩展
  training engine、optimizer bridge、accumulator v3 及受影响回归。
- step trace 明确覆盖 18 个逻辑边界。scaled micro-gradient 的真实尺度统计副本不改变正式
  `unscale_` 状态机；global finite 决策后才暂存 raw/U/clip；只有 optimizer 确实更新后才
  事务提交长期累计、movement 和 scheduler。skip 消费 batch 并推进 attempt/cursor/RNG/
  scaler/skip count，但不推进 successful step、scheduler、贡献或 movement。
- SGD fixture 覆盖多参数组与逐步实际学习率；AdamW 同时覆盖零/非零 weight decay、更新前后
  moments、`total = data + decay`、data/total net movement 和 accumulator v3 roundtrip。
  `actual_update_raw_importance` 仅保持为独立诊断，不重解释 gradient-space U。
- Tiny Transformer 统计开/关逐步比较 mean loss、optimizer gradient/state、参数、scheduler、
  cursor 与 RNG；真实 CPU GradScaler 覆盖 finite→skip→finite，非平凡 clip 仅应用一次。
- S1.5 handoff 同时验证历史 artifact 自洽、当前 checkout 语义重建与 producer→consumer source
  diff allowlist；共享依赖漂移、角色联合重哈希和 replay 输入篡改均 fail-closed。
- 修正提交 `ceb202ec3278c60111a31d82692b36eb98de6a40` 为所有 S1.6 trace builder 增加
  局部确定性 runtime transaction：固定并恢复 Python、NumPy、Torch CPU RNG、线程数和
  deterministic-algorithms 状态；仅在 CUDA 已初始化时读写 CUDA RNG，CPU formal 不触发
  CUDA 初始化。三次连续 build 与不同调用者线程数产生完全相同角色，并且不污染调用者状态。
- 子代理最终本地验收：五文件专项 `44 passed`，Stage 1 受影响宽回归 `113 passed`；
  `compileall` 与 `git diff --check` 均通过。主代理另行复跑连续构建/RNG 隔离测试，并在内存中
  构建全部 5 个角色，核对 13 项要求、118 行比较、37 个 source digest、离线重放和 8 份
  Schema，均通过。

## 失败尝试与修正

- `formal-20260815-s16-v1-r1` 在本地 SSH 包装器误设 10 秒超时后被中止；未发布 evidence，
  仅在 `DATA_ROOT/tmp/stage1-s1-6/d214c42f5fc02d5843b3b73afb1fb7dde90371ab/` 保留失败
  临时目录。该失败属于操作层超时，不作为 Gate 结果。
- `formal-20260815-s16-v1-r2` 在服务器定向回归阶段 fail-closed，未发布 evidence。精确回归
  为 `41 passed, 1 failed`；首轮 build 与 offline rebuild 仅在 SGD 两步和 clip 一步的
  `control_state_hash` 不同，数值、参数、optimizer、accumulator 与 Tiny trace 全部一致。
  根因是这些 fixture 的控制状态哈希继承了调用者 RNG。上述 `ceb202ec...` 修正后，连续三次
  build、线程数切换和全局状态无污染测试均通过；没有放宽哈希或 replay 规则。
- 两次失败都没有进入不可变正式 evidence 目录；其临时目录保留用于审计，不冒充成功 attempt。

## 服务器正式执行

- 唯一成功 attempt：
  `evidence/stage1/s1-6-formal/ceb202ec3278c60111a31d82692b36eb98de6a40/formal-20260815-s16-v1-r3/`。
- frozen 环境回归：`44 passed in 17.38s`；formal direct checks：`13/13 PASS`；
  `G1-STEP=PASS`，13 项 requirement 全部为真。执行后服务器 Git 工作树干净。
- direct checks 覆盖 S1.5 handoff/当前语义重放、fixture hash、独立 oracle 隔离、多组 SGD、
  非平凡 clip、AdamW delta 分解、累计恒等式、actual-update 诊断、skip 原子性、Tiny
  Transformer 无扰动、图表精确投影和离线重放。
- 主代理发布后从 immutable 目录重新读取全部角色：index、validation、replay 和 5 个角色的
  文件哈希、自哈希及交叉引用全部一致；当前代码重建与发布角色完全相等，offline replay 为
  `PASS`；8 份 Draft 2020-12 Schema 全部验证通过。
- comparison table 共 118 行，source map 共 37 个文件。四组 CSV/SVG 均由角色数据精确投影，
  上游 index SHA 和 Gate hash 与 S1.5 当前 handoff 完全一致。

## 正式证据与哈希

- `index.json` 文件 SHA-256：
  `9b25d016bdfb82a75d46b3fa89432211314e72e9a63522c772c2da1a764e69bc`；
  index artifact hash：
  `c73c855ce36fb36990549238ee43390142cdad0d203b687783f52a8e46c73fca`。
- `g1-step-record.json` 文件 SHA-256：
  `9a3fec415d484c3f51490b046ad99e2d501362365b999cdff557ec17319d7611`；
  Gate artifact hash：
  `872775ca7b74d5948019bb47ee070ec5f5cf81dc2127ca16d805944787a61a64`。
- `step-report.json` 文件 SHA-256：
  `11006cdde977063f9efb967a0e402ceba768f73180febaa45a25d75ff38172a3`；
  report hash：
  `425a9a67e519f5b313c139e5848ca3ae66557c090cf7a44365a0453b7899843a`。
- `oracle-bundle.json` 文件 SHA-256：
  `9f47adbad781fabd786ec0c684889a0142d54c532bfa097584bdee68d5b16a81`；
  oracle hash：
  `b18d00dad37b216c747e8a991b3632d0387cca988a86f0577012ae5b15d8fe63`。
- `trace-bundle.json` 文件 SHA-256：
  `487dde57fc72ba54f41eceeb9d6912049f1ba38323aeae70e35c6df75736d8cd`；
  trace hash：
  `e061fc2f3508107412f71430faa1a484a3844e9de40ef3804fadb6bce1c28448`。
- `comparison-table.json` 文件 SHA-256：
  `c816dfaf7ad12adeaf52a7b2e85becd6847e70f5ac0b79448d2813ecab8c6db1`；
  table hash：
  `5b26f2f14442deba7d5cd7ed0d4a370d2ef204cdc2d4f1d73347d30ef6d3bdb1`。
- `replay-validation.json` 文件 SHA-256：
  `dbce62465af88021fd0f2eadca4beaf815c6b4b7c4731aef47c37ca49063d2f8`；
  replay hash：
  `fdbb1a95085cba30fae9fe719eb595df654021d097a1b0e6c8e2bcde311c7da2`。
- `validation.json` 文件 SHA-256：
  `bc56cb3bb82d7fff1eb865705fe811589a38fe51afb1d05d2cc6a3b9f9bf3a29`；
  validation artifact hash：
  `782561fe5081ac5806815bebf583762bcfeed5434d9dcd986539498c3e1c224b`。
- CSV 文件 SHA-256：`accumulator-identities.csv`
  `1c6d6ba015635c8433815138de0ead14747d1a06e0d3cc36613944e663107676`，
  `movement-stack.csv` `9b1ee9bafde62ab2eef137a43736a80425ef3d7e2279688e866d423c98d9f95f`，
  `score-masses.csv` `651508a1d9c3fe1391c0e2e25ff2e000eb977315f7ff91fc3c26ca1fc1035f4c`，
  `trajectory-parity.csv` `d238152960fcf2c543721f11b7505430bc438923736f45fdf985aea645dbbe45`。
- SVG 文件 SHA-256：`accumulator-identities.svg`
  `90d47c1aab34e430e75d669ed66a80ffe0fdb09d14ac0015077741e754f15734`，
  `movement-stack.svg` `8d201125ea089fb03b76aa5cc32435cbdb9224d2090b5e069168f2e2ec778a74`，
  `score-masses.svg` `fe7201721ab57472514fade4684a788699b5cacc37d874d8ef70e68a34235972`，
  `trajectory-parity.svg` `7f2fb32cf7f45b5766bb9bd7d304d8483e036c174c06a4df0102bf5d36314f7c`。

## 结论与后续边界

- S1.6 正式验收通过，`G1-STEP` 解锁 `stage1.07_single_gpu_pythia14m`。
- 后续若修改 18 边界顺序、optimizer/GradScaler bridge、累计字段或迁移、movement 定义、
  skip/clip/finite 决策、S1.5 handoff、fixture 或 S1.6 Schema，必须重新生成本 Gate 并逐级
  重绑下游证据。
- 本次结果只证明单进程 CPU 解析 fixture 和 Tiny Transformer 语义；Pythia-14M、真实数据、
  单卡 GPU、显存预算与长链路轨迹必须由 S1.7 独立验证，不能由本 Gate 代替。
